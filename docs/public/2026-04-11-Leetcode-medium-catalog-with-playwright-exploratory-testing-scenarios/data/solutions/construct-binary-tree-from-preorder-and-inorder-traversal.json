{
  "generatedAt": "2026-04-11",
  "problemId": "construct-binary-tree-from-preorder-and-inorder-traversal",
  "problemTitle": "Construct Binary Tree from Preorder and Inorder Traversal",
  "problemNumber": 105,
  "summary": {
    "hasSolutions": true,
    "languageIds": [
      "python",
      "cpp",
      "java",
      "go",
      "typescript",
      "javascript",
      "csharp",
      "c",
      "kotlin"
    ],
    "languageLabels": [
      "Python",
      "C++",
      "Java",
      "Go",
      "TypeScript",
      "JavaScript",
      "C#",
      "C",
      "Kotlin"
    ],
    "solutionCount": 18,
    "writeupCount": 3,
    "primaryLanguageId": "python"
  },
  "languages": [
    {
      "id": "python",
      "label": "Python",
      "sourceCount": 4,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-python-0105-construct-binary-tree-from-preorder-and-inorder-traversal.py",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/python/0105-construct-binary-tree-from-preorder-and-inorder-traversal.py",
          "localPath": "neetcode-gh_leetcode/python/0105-construct-binary-tree-from-preorder-and-inorder-traversal.py",
          "relativePath": "python/0105-construct-binary-tree-from-preorder-and-inorder-traversal.py",
          "languageId": "python",
          "languageLabel": "Python",
          "coverageType": "code",
          "code": "class Solution:\n    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:\n        if not preorder or not inorder:\n            return None\n\n        root = TreeNode(preorder[0])\n        mid = inorder.index(preorder[0])\n        root.left = self.buildTree(preorder[1 : mid + 1], inorder[:mid])\n        root.right = self.buildTree(preorder[mid + 1 :], inorder[mid + 1 :])\n        return root\n",
          "originalCode": "class Solution:\n    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:\n        if not preorder or not inorder:\n            return None\n\n        root = TreeNode(preorder[0])\n        mid = inorder.index(preorder[0])\n        root.left = self.buildTree(preorder[1 : mid + 1], inorder[:mid])\n        root.right = self.buildTree(preorder[mid + 1 :], inorder[mid + 1 :])\n        return root\n",
          "reviewedCode": "# [Reader note] Entry point\n# Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nclass Solution:\n    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:\n        # [Reader note] Decision point\n        # Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if not preorder or not inorder:\n            return None\n\n        # [Reader note] State setup\n        # This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        root = TreeNode(preorder[0])\n        mid = inorder.index(preorder[0])\n        root.left = self.buildTree(preorder[1 : mid + 1], inorder[:mid])\n        root.right = self.buildTree(preorder[mid + 1 :], inorder[mid + 1 :])\n        # [Reader note] Return\n        # The final return exposes the result after the traversal and state updates have settled.\n        return root",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 6,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 3,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 10,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "kamyu104_leetcode-solutions-python-construct-binary-tree-from-preorder-and-inorder-traversal.py",
          "repository": "kamyu104_LeetCode-Solutions",
          "repositoryUrl": "https://github.com/kamyu104/LeetCode-Solutions",
          "sourceUrl": "https://github.com/kamyu104/LeetCode-Solutions/blob/master/Python/construct-binary-tree-from-preorder-and-inorder-traversal.py",
          "localPath": "kamyu104_LeetCode-Solutions/Python/construct-binary-tree-from-preorder-and-inorder-traversal.py",
          "relativePath": "Python/construct-binary-tree-from-preorder-and-inorder-traversal.py",
          "languageId": "python",
          "languageLabel": "Python",
          "coverageType": "indexed-code",
          "code": "# Time:  O(n)\n# Space: O(n)\n\nclass TreeNode(object):\n    def __init__(self, x):\n        self.val = x\n        self.left = None\n        self.right = None\n\nclass Solution(object):\n    # @param preorder, a list of integers\n    # @param inorder, a list of integers\n    # @return a tree node\n    def buildTree(self, preorder, inorder):\n        lookup = {}\n        for i, num in enumerate(inorder):\n            lookup[num] = i\n        return self.buildTreeRecu(lookup, preorder, inorder, 0, 0, len(inorder))\n\n    def buildTreeRecu(self, lookup, preorder, inorder, pre_start, in_start, in_end):\n        if in_start == in_end:\n            return None\n        node = TreeNode(preorder[pre_start])\n        i = lookup[preorder[pre_start]]\n        node.left = self.buildTreeRecu(lookup, preorder, inorder, pre_start + 1, in_start, i)\n        node.right = self.buildTreeRecu(lookup, preorder, inorder, pre_start + 1 + i - in_start, i + 1, in_end)\n        return node\n\n\n# time: O(n)\n# space: O(n)\nclass Solution2(object):\n    def buildTree(self, preorder, inorder):\n        \"\"\"\n        :type preorder: List[int]\n        :type inorder: List[int]\n        :rtype: TreeNode\n        \"\"\"\n        preorder_iterator = iter(preorder)\n        inorder_lookup = {n: i for i, n in enumerate(inorder)}\n        \n        def helper(start, end):\n            if start > end:\n                return None\n            \n            root_val = next(preorder_iterator)\n            root = TreeNode(root_val)\n            idx = inorder_lookup[root_val]\n            root.left = helper(start, idx-1)\n            root.right = helper(idx+1, end)\n            return root\n\n        return helper(0, len(inorder)-1)\n",
          "originalCode": "# Time:  O(n)\n# Space: O(n)\n\nclass TreeNode(object):\n    def __init__(self, x):\n        self.val = x\n        self.left = None\n        self.right = None\n\nclass Solution(object):\n    # @param preorder, a list of integers\n    # @param inorder, a list of integers\n    # @return a tree node\n    def buildTree(self, preorder, inorder):\n        lookup = {}\n        for i, num in enumerate(inorder):\n            lookup[num] = i\n        return self.buildTreeRecu(lookup, preorder, inorder, 0, 0, len(inorder))\n\n    def buildTreeRecu(self, lookup, preorder, inorder, pre_start, in_start, in_end):\n        if in_start == in_end:\n            return None\n        node = TreeNode(preorder[pre_start])\n        i = lookup[preorder[pre_start]]\n        node.left = self.buildTreeRecu(lookup, preorder, inorder, pre_start + 1, in_start, i)\n        node.right = self.buildTreeRecu(lookup, preorder, inorder, pre_start + 1 + i - in_start, i + 1, in_end)\n        return node\n\n\n# time: O(n)\n# space: O(n)\nclass Solution2(object):\n    def buildTree(self, preorder, inorder):\n        \"\"\"\n        :type preorder: List[int]\n        :type inorder: List[int]\n        :rtype: TreeNode\n        \"\"\"\n        preorder_iterator = iter(preorder)\n        inorder_lookup = {n: i for i, n in enumerate(inorder)}\n        \n        def helper(start, end):\n            if start > end:\n                return None\n            \n            root_val = next(preorder_iterator)\n            root = TreeNode(root_val)\n            idx = inorder_lookup[root_val]\n            root.left = helper(start, idx-1)\n            root.right = helper(idx+1, end)\n            return root\n\n        return helper(0, len(inorder)-1)\n",
          "reviewedCode": "# Time:  O(n)\n# Space: O(n)\n\nclass TreeNode(object):\n    # [Reader note] Entry point\n    # Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\n    def __init__(self, x):\n        # [Reader note] State setup\n        # This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        self.val = x\n        self.left = None\n        self.right = None\n\nclass Solution(object):\n    # @param preorder, a list of integers\n    # @param inorder, a list of integers\n    # @return a tree node\n    def buildTree(self, preorder, inorder):\n        lookup = {}\n        # [Reader note] Main pass\n        # This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for i, num in enumerate(inorder):\n            lookup[num] = i\n        return self.buildTreeRecu(lookup, preorder, inorder, 0, 0, len(inorder))\n\n    def buildTreeRecu(self, lookup, preorder, inorder, pre_start, in_start, in_end):\n        # [Reader note] Decision point\n        # Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if in_start == in_end:\n            return None\n        node = TreeNode(preorder[pre_start])\n        i = lookup[preorder[pre_start]]\n        node.left = self.buildTreeRecu(lookup, preorder, inorder, pre_start + 1, in_start, i)\n        node.right = self.buildTreeRecu(lookup, preorder, inorder, pre_start + 1 + i - in_start, i + 1, in_end)\n        return node\n\n\n# time: O(n)\n# space: O(n)\nclass Solution2(object):\n    def buildTree(self, preorder, inorder):\n        \"\"\"\n        :type preorder: List[int]\n        :type inorder: List[int]\n        :rtype: TreeNode\n        \"\"\"\n        preorder_iterator = iter(preorder)\n        inorder_lookup = {n: i for i, n in enumerate(inorder)}\n        \n        def helper(start, end):\n            if start > end:\n                return None\n            \n            root_val = next(preorder_iterator)\n            root = TreeNode(root_val)\n            idx = inorder_lookup[root_val]\n            root.left = helper(start, idx-1)\n            root.right = helper(idx+1, end)\n            return root\n\n        # [Reader note] Return\n        # The final return exposes the result after the traversal and state updates have settled.\n        return helper(0, len(inorder)-1)",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [
            "Time:  O(n)",
            "Space: O(n)"
          ],
          "annotations": [
            {
              "line": 5,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 6,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 16,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 21,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 53,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "doocs_leetcode-solution-0100-0199-0105.construct-binary-tree-from-preorder-and-inorder-traversal-solution.py",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/Solution.py",
          "localPath": "doocs_leetcode/solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.py",
          "relativePath": "solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.py",
          "languageId": "python",
          "languageLabel": "Python",
          "coverageType": "code+explanation",
          "code": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:\n        def dfs(i: int, j: int, n: int) -> Optional[TreeNode]:\n            if n <= 0:\n                return None\n            v = preorder[i]\n            k = d[v]\n            l = dfs(i + 1, j, k - j)\n            r = dfs(i + 1 + k - j, k + 1, n - k + j - 1)\n            return TreeNode(v, l, r)\n\n        d = {v: i for i, v in enumerate(inorder)}\n        return dfs(0, 0, len(preorder))\n",
          "originalCode": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:\n        def dfs(i: int, j: int, n: int) -> Optional[TreeNode]:\n            if n <= 0:\n                return None\n            v = preorder[i]\n            k = d[v]\n            l = dfs(i + 1, j, k - j)\n            r = dfs(i + 1 + k - j, k + 1, n - k + j - 1)\n            return TreeNode(v, l, r)\n\n        d = {v: i for i, v in enumerate(inorder)}\n        return dfs(0, 0, len(preorder))\n",
          "reviewedCode": "# Definition for a binary tree node.\n# class TreeNode:\n# [Reader note] Entry point\n# Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\n#     def __init__(self, val=0, left=None, right=None):\n# [Reader note] State setup\n# This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:\n        def dfs(i: int, j: int, n: int) -> Optional[TreeNode]:\n            if n <= 0:\n                return None\n            v = preorder[i]\n            k = d[v]\n            l = dfs(i + 1, j, k - j)\n            r = dfs(i + 1 + k - j, k + 1, n - k + j - 1)\n            return TreeNode(v, l, r)\n\n        # [Reader note] Main pass\n        # This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        d = {v: i for i, v in enumerate(inorder)}\n        # [Reader note] Return\n        # The final return exposes the result after the traversal and state updates have settled.\n        return dfs(0, 0, len(preorder))",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 3,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 4,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 18,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 19,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "haoel_leetcode-algorithms-python-constructbinarytreefrompreorderandinordertraversal-buildtree.py",
          "repository": "haoel_leetcode",
          "repositoryUrl": "https://github.com/haoel/leetcode",
          "sourceUrl": "https://github.com/haoel/leetcode/blob/master/algorithms/python/ConstructBinaryTreeFromPreorderAndInorderTraversal/buildTree.py",
          "localPath": "haoel_leetcode/algorithms/python/ConstructBinaryTreeFromPreorderAndInorderTraversal/buildTree.py",
          "relativePath": "algorithms/python/ConstructBinaryTreeFromPreorderAndInorderTraversal/buildTree.py",
          "languageId": "python",
          "languageLabel": "Python",
          "coverageType": "indexed-code",
          "code": "def buildTree(self, preorder, inorder):\n    if inorder:\n        i = inorder.index(preorder.pop(0))\n        root = TreeNode(inorder[i])\n        root.left = self.buildTree(preorder, inorder[:i])\n        root.right = self.buildTree(preorder, inorder[i+1:])\n        return root",
          "originalCode": "def buildTree(self, preorder, inorder):\n    if inorder:\n        i = inorder.index(preorder.pop(0))\n        root = TreeNode(inorder[i])\n        root.left = self.buildTree(preorder, inorder[:i])\n        root.right = self.buildTree(preorder, inorder[i+1:])\n        return root",
          "reviewedCode": "# [Reader note] Entry point\n# Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\ndef buildTree(self, preorder, inorder):\n    # [Reader note] Decision point\n    # Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n    if inorder:\n        # [Reader note] State setup\n        # This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        i = inorder.index(preorder.pop(0))\n        root = TreeNode(inorder[i])\n        root.left = self.buildTree(preorder, inorder[:i])\n        root.right = self.buildTree(preorder, inorder[i+1:])\n        # [Reader note] Return\n        # The final return exposes the result after the traversal and state updates have settled.\n        return root",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 3,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 2,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 7,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    },
    {
      "id": "cpp",
      "label": "C++",
      "sourceCount": 5,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-cpp-0105-construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/cpp/0105-construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "localPath": "neetcode-gh_leetcode/cpp/0105-construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "relativePath": "cpp/0105-construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "code",
          "code": "/*\n    Given 2 integer arrays preorder & inorder, construct & return the binary tree\n    Ex. preorder = [3,9,20,15,7], inorder = [9,3,15,20,7] -> [3,9,20,null,null,15,7]\n\n    Preorder dictates nodes, inorder dictates subtrees (preorder values, inorder positions)\n\n    Time: O(n)\n    Space: O(n)\n*/\n\n/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode() : val(0), left(nullptr), right(nullptr) {}\n *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}\n * };\n */\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        int index = 0;\n        return build(preorder, inorder, index, 0, inorder.size() - 1);\n    }\nprivate:\n    TreeNode* build(vector<int>& preorder, vector<int>& inorder, int& index, int i, int j) {\n        if (i > j) {\n            return NULL;\n        }\n        \n        TreeNode* root = new TreeNode(preorder[index]);\n        \n        int split = 0;\n        for (int i = 0; i < inorder.size(); i++) {\n            if (preorder[index] == inorder[i]) {\n                split = i;\n                break;\n            }\n        }\n        index++;\n        \n        root->left = build(preorder, inorder, index, i, split - 1);\n        root->right = build(preorder, inorder, index, split + 1, j);\n        \n        return root;\n    }\n};\n",
          "originalCode": "/*\n    Given 2 integer arrays preorder & inorder, construct & return the binary tree\n    Ex. preorder = [3,9,20,15,7], inorder = [9,3,15,20,7] -> [3,9,20,null,null,15,7]\n\n    Preorder dictates nodes, inorder dictates subtrees (preorder values, inorder positions)\n\n    Time: O(n)\n    Space: O(n)\n*/\n\n/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode() : val(0), left(nullptr), right(nullptr) {}\n *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}\n * };\n */\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        int index = 0;\n        return build(preorder, inorder, index, 0, inorder.size() - 1);\n    }\nprivate:\n    TreeNode* build(vector<int>& preorder, vector<int>& inorder, int& index, int i, int j) {\n        if (i > j) {\n            return NULL;\n        }\n        \n        TreeNode* root = new TreeNode(preorder[index]);\n        \n        int split = 0;\n        for (int i = 0; i < inorder.size(); i++) {\n            if (preorder[index] == inorder[i]) {\n                split = i;\n                break;\n            }\n        }\n        index++;\n        \n        root->left = build(preorder, inorder, index, i, split - 1);\n        root->right = build(preorder, inorder, index, split + 1, j);\n        \n        return root;\n    }\n};\n",
          "reviewedCode": "/*\n    Given 2 integer arrays preorder & inorder, construct & return the binary tree\n    Ex. preorder = [3,9,20,15,7], inorder = [9,3,15,20,7] -> [3,9,20,null,null,15,7]\n\n    Preorder dictates nodes, inorder dictates subtrees (preorder values, inorder positions)\n\n    Time: O(n)\n    Space: O(n)\n*/\n\n/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode() : val(0), left(nullptr), right(nullptr) {}\n *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}\n * };\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        int index = 0;\n        return build(preorder, inorder, index, 0, inorder.size() - 1);\n    }\nprivate:\n    TreeNode* build(vector<int>& preorder, vector<int>& inorder, int& index, int i, int j) {\n        if (i > j) {\n            return NULL;\n        }\n        \n        TreeNode* root = new TreeNode(preorder[index]);\n        \n        int split = 0;\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (int i = 0; i < inorder.size(); i++) {\n            // [Reader note] Decision point\n            // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n            if (preorder[index] == inorder[i]) {\n                split = i;\n                break;\n            }\n        }\n        index++;\n        \n        root->left = build(preorder, inorder, index, i, split - 1);\n        root->right = build(preorder, inorder, index, split + 1, j);\n        \n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return root;\n    }\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [
            "Time: O(n)",
            "Space: O(n)"
          ],
          "annotations": [
            {
              "line": 22,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 25,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 37,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 38,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 48,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "kamyu104_leetcode-solutions-c-construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "repository": "kamyu104_LeetCode-Solutions",
          "repositoryUrl": "https://github.com/kamyu104/LeetCode-Solutions",
          "sourceUrl": "https://github.com/kamyu104/LeetCode-Solutions/blob/master/C%2B%2B/construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "localPath": "kamyu104_LeetCode-Solutions/C++/construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "relativePath": "C++/construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "indexed-code",
          "code": "// Time:  O(n)\n// Space: O(n)\n\n/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n * };\n */\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        unordered_map<int, size_t> in_entry_idx_map;\n        for (size_t i = 0; i < inorder.size(); ++i) {\n            in_entry_idx_map.emplace(inorder[i], i);\n        }\n        return ReconstructPreInOrdersHelper(preorder, 0, preorder.size(), inorder, 0, inorder.size(),\n                                            in_entry_idx_map);\n    }\n\n    // Reconstructs the binary tree from pre[pre_s : pre_e - 1] and\n    // in[in_s : in_e - 1].\n    TreeNode *ReconstructPreInOrdersHelper(const vector<int>& preorder, size_t pre_s, size_t pre_e,\n                                           const vector<int>& inorder, size_t in_s, size_t in_e,\n                                           const unordered_map<int, size_t>& in_entry_idx_map) {\n        if (pre_s == pre_e || in_s == in_e) {\n            return nullptr;\n        }\n\n        auto idx = in_entry_idx_map.at(preorder[pre_s]);\n        auto left_tree_size = idx - in_s;\n\n        auto node = new TreeNode(preorder[pre_s]);\n        node->left = ReconstructPreInOrdersHelper(preorder, pre_s + 1, pre_s + 1 + left_tree_size,\n                                                  inorder, in_s, idx, in_entry_idx_map);\n        node->right = ReconstructPreInOrdersHelper(preorder, pre_s + 1 + left_tree_size, pre_e,\n                                                   inorder, idx + 1, in_e, in_entry_idx_map);\n        return node;\n    }\n};\n",
          "originalCode": "// Time:  O(n)\n// Space: O(n)\n\n/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n * };\n */\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        unordered_map<int, size_t> in_entry_idx_map;\n        for (size_t i = 0; i < inorder.size(); ++i) {\n            in_entry_idx_map.emplace(inorder[i], i);\n        }\n        return ReconstructPreInOrdersHelper(preorder, 0, preorder.size(), inorder, 0, inorder.size(),\n                                            in_entry_idx_map);\n    }\n\n    // Reconstructs the binary tree from pre[pre_s : pre_e - 1] and\n    // in[in_s : in_e - 1].\n    TreeNode *ReconstructPreInOrdersHelper(const vector<int>& preorder, size_t pre_s, size_t pre_e,\n                                           const vector<int>& inorder, size_t in_s, size_t in_e,\n                                           const unordered_map<int, size_t>& in_entry_idx_map) {\n        if (pre_s == pre_e || in_s == in_e) {\n            return nullptr;\n        }\n\n        auto idx = in_entry_idx_map.at(preorder[pre_s]);\n        auto left_tree_size = idx - in_s;\n\n        auto node = new TreeNode(preorder[pre_s]);\n        node->left = ReconstructPreInOrdersHelper(preorder, pre_s + 1, pre_s + 1 + left_tree_size,\n                                                  inorder, in_s, idx, in_entry_idx_map);\n        node->right = ReconstructPreInOrdersHelper(preorder, pre_s + 1 + left_tree_size, pre_e,\n                                                   inorder, idx + 1, in_e, in_entry_idx_map);\n        return node;\n    }\n};\n",
          "reviewedCode": "// Time:  O(n)\n// Space: O(n)\n\n/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n * };\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        unordered_map<int, size_t> in_entry_idx_map;\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        for (size_t i = 0; i < inorder.size(); ++i) {\n            in_entry_idx_map.emplace(inorder[i], i);\n        }\n        return ReconstructPreInOrdersHelper(preorder, 0, preorder.size(), inorder, 0, inorder.size(),\n                                            in_entry_idx_map);\n    }\n\n    // Reconstructs the binary tree from pre[pre_s : pre_e - 1] and\n    // in[in_s : in_e - 1].\n    TreeNode *ReconstructPreInOrdersHelper(const vector<int>& preorder, size_t pre_s, size_t pre_e,\n                                           const vector<int>& inorder, size_t in_s, size_t in_e,\n                                           const unordered_map<int, size_t>& in_entry_idx_map) {\n        // [Reader note] Decision point\n        // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if (pre_s == pre_e || in_s == in_e) {\n            return nullptr;\n        }\n\n        auto idx = in_entry_idx_map.at(preorder[pre_s]);\n        auto left_tree_size = idx - in_s;\n\n        auto node = new TreeNode(preorder[pre_s]);\n        node->left = ReconstructPreInOrdersHelper(preorder, pre_s + 1, pre_s + 1 + left_tree_size,\n                                                  inorder, in_s, idx, in_entry_idx_map);\n        node->right = ReconstructPreInOrdersHelper(preorder, pre_s + 1 + left_tree_size, pre_e,\n                                                   inorder, idx + 1, in_e, in_entry_idx_map);\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return node;\n    }\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [
            "Time:  O(n)",
            "Space: O(n)"
          ],
          "annotations": [
            {
              "line": 13,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 17,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 29,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 41,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "doocs_leetcode-solution-0100-0199-0105.construct-binary-tree-from-preorder-and-inorder-traversal-solution.cpp",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/Solution.cpp",
          "localPath": "doocs_leetcode/solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.cpp",
          "relativePath": "solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "code+explanation",
          "code": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode() : val(0), left(nullptr), right(nullptr) {}\n *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}\n * };\n */\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        int n = preorder.size();\n        unordered_map<int, int> d;\n        for (int i = 0; i < n; ++i) {\n            d[inorder[i]] = i;\n        }\n        function<TreeNode*(int, int, int)> dfs = [&](int i, int j, int n) -> TreeNode* {\n            if (n <= 0) {\n                return nullptr;\n            }\n            int v = preorder[i];\n            int k = d[v];\n            TreeNode* l = dfs(i + 1, j, k - j);\n            TreeNode* r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n            return new TreeNode(v, l, r);\n        };\n        return dfs(0, 0, n);\n    }\n};",
          "originalCode": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode() : val(0), left(nullptr), right(nullptr) {}\n *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}\n * };\n */\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        int n = preorder.size();\n        unordered_map<int, int> d;\n        for (int i = 0; i < n; ++i) {\n            d[inorder[i]] = i;\n        }\n        function<TreeNode*(int, int, int)> dfs = [&](int i, int j, int n) -> TreeNode* {\n            if (n <= 0) {\n                return nullptr;\n            }\n            int v = preorder[i];\n            int k = d[v];\n            TreeNode* l = dfs(i + 1, j, k - j);\n            TreeNode* r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n            return new TreeNode(v, l, r);\n        };\n        return dfs(0, 0, n);\n    }\n};",
          "reviewedCode": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode() : val(0), left(nullptr), right(nullptr) {}\n *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}\n * };\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nclass Solution {\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        int n = preorder.size();\n        unordered_map<int, int> d;\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (int i = 0; i < n; ++i) {\n            d[inorder[i]] = i;\n        }\n        function<TreeNode*(int, int, int)> dfs = [&](int i, int j, int n) -> TreeNode* {\n            // [Reader note] Decision point\n            // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n            if (n <= 0) {\n                return nullptr;\n            }\n            int v = preorder[i];\n            int k = d[v];\n            TreeNode* l = dfs(i + 1, j, k - j);\n            TreeNode* r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n            return new TreeNode(v, l, r);\n        };\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return dfs(0, 0, n);\n    }\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 12,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 15,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 17,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 21,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 30,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "wisdompeak_leetcode-tree-105.construct-binary-tree-from-preorder-and-inorder-traversal-105.construct-binary-tree-from-preorder-and-inorder-traversal.cpp",
          "repository": "wisdompeak_LeetCode",
          "repositoryUrl": "https://github.com/wisdompeak/LeetCode",
          "sourceUrl": "https://github.com/wisdompeak/LeetCode/blob/master/Tree/105.Construct-Binary-Tree-from-Preorder-and-Inorder-Traversal/105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal.cpp",
          "localPath": "wisdompeak_LeetCode/Tree/105.Construct-Binary-Tree-from-Preorder-and-Inorder-Traversal/105.Construct Binary Tree from Preorder and Inorder Traversal.cpp",
          "relativePath": "Tree/105.Construct-Binary-Tree-from-Preorder-and-Inorder-Traversal/105.Construct Binary Tree from Preorder and Inorder Traversal.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "code+readme",
          "code": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n * };\n */\nclass Solution {\n    unordered_map<int,int>Map;\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) \n    {\n        if (preorder.size()==0) return NULL;\n        \n        for (int i=0; i<inorder.size(); i++)\n            Map[inorder[i]]=i;\n        \n        TreeNode* root = DFS(preorder,0,preorder.size()-1,inorder,0,inorder.size()-1);\n        \n        return root;\n    }\n    \n    TreeNode* DFS(vector<int>& preorder, int a, int b, vector<int>& inorder, int m, int n)\n    {\n        if (a>b)\n            return NULL;\n        \n        TreeNode* root = new TreeNode(preorder[a]);\n        \n        int pos = Map[preorder[a]];\n        root->left = DFS(preorder,a+1,a+pos-m,inorder,m,pos-1);\n        root->right = DFS(preorder,a+pos-m+1,b,inorder,pos+1,n);\n        return root;\n    }\n};\n",
          "originalCode": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n * };\n */\nclass Solution {\n    unordered_map<int,int>Map;\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) \n    {\n        if (preorder.size()==0) return NULL;\n        \n        for (int i=0; i<inorder.size(); i++)\n            Map[inorder[i]]=i;\n        \n        TreeNode* root = DFS(preorder,0,preorder.size()-1,inorder,0,inorder.size()-1);\n        \n        return root;\n    }\n    \n    TreeNode* DFS(vector<int>& preorder, int a, int b, vector<int>& inorder, int m, int n)\n    {\n        if (a>b)\n            return NULL;\n        \n        TreeNode* root = new TreeNode(preorder[a]);\n        \n        int pos = Map[preorder[a]];\n        root->left = DFS(preorder,a+1,a+pos-m,inorder,m,pos-1);\n        root->right = DFS(preorder,a+pos-m+1,b,inorder,pos+1,n);\n        return root;\n    }\n};\n",
          "reviewedCode": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     TreeNode *left;\n *     TreeNode *right;\n *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n * };\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nclass Solution {\n    unordered_map<int,int>Map;\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) \n    {\n        if (preorder.size()==0) return NULL;\n        \n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        for (int i=0; i<inorder.size(); i++)\n            Map[inorder[i]]=i;\n        \n        TreeNode* root = DFS(preorder,0,preorder.size()-1,inorder,0,inorder.size()-1);\n        \n        return root;\n    }\n    \n    TreeNode* DFS(vector<int>& preorder, int a, int b, vector<int>& inorder, int m, int n)\n    {\n        // [Reader note] Decision point\n        // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if (a>b)\n            return NULL;\n        \n        TreeNode* root = new TreeNode(preorder[a]);\n        \n        int pos = Map[preorder[a]];\n        root->left = DFS(preorder,a+1,a+pos-m,inorder,m,pos-1);\n        root->right = DFS(preorder,a+pos-m+1,b,inorder,pos+1,n);\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return root;\n    }\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 10,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 17,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 27,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 35,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "haoel_leetcode-algorithms-cpp-constructbinarytreefrompreorderandinordertraversal-constructbinarytreefrompreorderandinordertraversal.cpp",
          "repository": "haoel_leetcode",
          "repositoryUrl": "https://github.com/haoel/leetcode",
          "sourceUrl": "https://github.com/haoel/leetcode/blob/master/algorithms/cpp/constructBinaryTreeFromPreorderAndInorderTraversal/constructBinaryTreeFromPreorderAndInorderTraversal.cpp",
          "localPath": "haoel_leetcode/algorithms/cpp/constructBinaryTreeFromPreorderAndInorderTraversal/constructBinaryTreeFromPreorderAndInorderTraversal.cpp",
          "relativePath": "algorithms/cpp/constructBinaryTreeFromPreorderAndInorderTraversal/constructBinaryTreeFromPreorderAndInorderTraversal.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "indexed-code",
          "code": "// Source : https://oj.leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n// Author : Hao Chen\n// Date   : 2014-07-09\n\n/********************************************************************************** \n* \n* Given preorder and inorder traversal of a tree, construct the binary tree.\n* \n* Note:\n* You may assume that duplicates do not exist in the tree.\n* \n*               \n**********************************************************************************/\n\n#include <stdio.h>\n#include <vector>\n#include <queue>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left;\n    TreeNode *right;\n    TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n};\n\nTreeNode *buildTree(vector<int>& preorder, int& preidx, vector<int>& inorder);\n\nTreeNode *buildTree(vector<int> &preorder, vector<int> &inorder) {\n    int preidx=0;\n    return buildTree(preorder, preidx, inorder);\n}\n\nTreeNode *buildTree(vector<int>& preorder, int& preidx, vector<int>& inorder) {\n\n    if (preorder.size()<=0 || inorder.size()<=0 ) return NULL;\n\n    TreeNode *root = new TreeNode(preorder[preidx]);\n    if (inorder.size()==1){\n        return root;\n    }\n\n    int i;\n    for(i=0; i<inorder.size(); i++){\n        if (inorder[i] == preorder[preidx]){\n            break;\n        }\n    }\n\n    //error: not found\n    if (i == inorder.size()) return NULL;\n\n    if (preidx+1 >= preorder.size()){\n        return root;\n    }\n\n    \n    vector<int> v(inorder.begin(), inorder.begin()+i);\n    if (v.size()>0) {\n        preidx++;\n        root->left = buildTree(preorder, preidx, v);\n    }\n\n    v.clear();\n    v.assign(inorder.begin()+i+1, inorder.end());\n    if (v.size()>0) {\n        preidx++;\n        root->right = buildTree(preorder, preidx, v);\n    }\n\n    return root;\n}\n\nvoid printTree_pre_order(TreeNode *root)\n{\n    if (root == NULL){\n        printf(\"# \");\n        return;\n    }\n    printf(\"%c \", root->val );\n\n    printTree_pre_order(root->left);\n    printTree_pre_order(root->right);\n}\n\nvoid printTree_in_order(TreeNode *root)\n{\n    if (root == NULL){\n        printf(\"# \");\n        return;\n    }\n\n    printTree_in_order(root->left);\n    printf(\"%c \", root->val );\n    printTree_in_order(root->right);\n}\n\n\nvoid printTree_level_order(TreeNode *root)\n{\n    queue<TreeNode*> q;\n    q.push(root);\n    while (q.size()>0){\n        TreeNode* n = q.front();\n        q.pop();\n        if (n==NULL){\n            printf(\"# \");\n            continue;\n        }\n        printf(\"%c \", n->val);\n        q.push(n->left);\n        q.push(n->right);\n    }\n    printf(\"\\n\");\n}\n\n\nint main()\n{\n    int pre_order[]={'F', 'B', 'A', 'D', 'C', 'E', 'G', 'I', 'H'};\n    int  in_order[]={'A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I'};\n    vector<int> preorder( pre_order, pre_order + 9 );\n    vector<int>  inorder(  in_order,  in_order + 9 );\n\n    TreeNode* tree = buildTree(preorder, inorder);\n\n    printTree_level_order(tree);\n    printTree_pre_order(tree);\n    printf(\"\\n\");\n    printTree_in_order(tree);\n    printf(\"\\n\");\n    \n    return 0;\n}\n",
          "originalCode": "// Source : https://oj.leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n// Author : Hao Chen\n// Date   : 2014-07-09\n\n/********************************************************************************** \n* \n* Given preorder and inorder traversal of a tree, construct the binary tree.\n* \n* Note:\n* You may assume that duplicates do not exist in the tree.\n* \n*               \n**********************************************************************************/\n\n#include <stdio.h>\n#include <vector>\n#include <queue>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left;\n    TreeNode *right;\n    TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n};\n\nTreeNode *buildTree(vector<int>& preorder, int& preidx, vector<int>& inorder);\n\nTreeNode *buildTree(vector<int> &preorder, vector<int> &inorder) {\n    int preidx=0;\n    return buildTree(preorder, preidx, inorder);\n}\n\nTreeNode *buildTree(vector<int>& preorder, int& preidx, vector<int>& inorder) {\n\n    if (preorder.size()<=0 || inorder.size()<=0 ) return NULL;\n\n    TreeNode *root = new TreeNode(preorder[preidx]);\n    if (inorder.size()==1){\n        return root;\n    }\n\n    int i;\n    for(i=0; i<inorder.size(); i++){\n        if (inorder[i] == preorder[preidx]){\n            break;\n        }\n    }\n\n    //error: not found\n    if (i == inorder.size()) return NULL;\n\n    if (preidx+1 >= preorder.size()){\n        return root;\n    }\n\n    \n    vector<int> v(inorder.begin(), inorder.begin()+i);\n    if (v.size()>0) {\n        preidx++;\n        root->left = buildTree(preorder, preidx, v);\n    }\n\n    v.clear();\n    v.assign(inorder.begin()+i+1, inorder.end());\n    if (v.size()>0) {\n        preidx++;\n        root->right = buildTree(preorder, preidx, v);\n    }\n\n    return root;\n}\n\nvoid printTree_pre_order(TreeNode *root)\n{\n    if (root == NULL){\n        printf(\"# \");\n        return;\n    }\n    printf(\"%c \", root->val );\n\n    printTree_pre_order(root->left);\n    printTree_pre_order(root->right);\n}\n\nvoid printTree_in_order(TreeNode *root)\n{\n    if (root == NULL){\n        printf(\"# \");\n        return;\n    }\n\n    printTree_in_order(root->left);\n    printf(\"%c \", root->val );\n    printTree_in_order(root->right);\n}\n\n\nvoid printTree_level_order(TreeNode *root)\n{\n    queue<TreeNode*> q;\n    q.push(root);\n    while (q.size()>0){\n        TreeNode* n = q.front();\n        q.pop();\n        if (n==NULL){\n            printf(\"# \");\n            continue;\n        }\n        printf(\"%c \", n->val);\n        q.push(n->left);\n        q.push(n->right);\n    }\n    printf(\"\\n\");\n}\n\n\nint main()\n{\n    int pre_order[]={'F', 'B', 'A', 'D', 'C', 'E', 'G', 'I', 'H'};\n    int  in_order[]={'A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I'};\n    vector<int> preorder( pre_order, pre_order + 9 );\n    vector<int>  inorder(  in_order,  in_order + 9 );\n\n    TreeNode* tree = buildTree(preorder, inorder);\n\n    printTree_level_order(tree);\n    printTree_pre_order(tree);\n    printf(\"\\n\");\n    printTree_in_order(tree);\n    printf(\"\\n\");\n    \n    return 0;\n}\n",
          "reviewedCode": "// Source : https://oj.leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n// Author : Hao Chen\n// Date   : 2014-07-09\n\n/********************************************************************************** \n* \n* Given preorder and inorder traversal of a tree, construct the binary tree.\n* \n* Note:\n* You may assume that duplicates do not exist in the tree.\n* \n*               \n**********************************************************************************/\n\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\n#include <stdio.h>\n#include <vector>\n#include <queue>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left;\n    TreeNode *right;\n    TreeNode(int x) : val(x), left(NULL), right(NULL) {}\n};\n\nTreeNode *buildTree(vector<int>& preorder, int& preidx, vector<int>& inorder);\n\nTreeNode *buildTree(vector<int> &preorder, vector<int> &inorder) {\n    // [Reader note] State setup\n    // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n    int preidx=0;\n    return buildTree(preorder, preidx, inorder);\n}\n\nTreeNode *buildTree(vector<int>& preorder, int& preidx, vector<int>& inorder) {\n\n    if (preorder.size()<=0 || inorder.size()<=0 ) return NULL;\n\n    TreeNode *root = new TreeNode(preorder[preidx]);\n    if (inorder.size()==1){\n        return root;\n    }\n\n    int i;\n    // [Reader note] Main pass\n    // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n    for(i=0; i<inorder.size(); i++){\n        // [Reader note] Decision point\n        // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if (inorder[i] == preorder[preidx]){\n            break;\n        }\n    }\n\n    //error: not found\n    if (i == inorder.size()) return NULL;\n\n    if (preidx+1 >= preorder.size()){\n        return root;\n    }\n\n    \n    vector<int> v(inorder.begin(), inorder.begin()+i);\n    if (v.size()>0) {\n        preidx++;\n        root->left = buildTree(preorder, preidx, v);\n    }\n\n    v.clear();\n    v.assign(inorder.begin()+i+1, inorder.end());\n    if (v.size()>0) {\n        preidx++;\n        root->right = buildTree(preorder, preidx, v);\n    }\n\n    return root;\n}\n\nvoid printTree_pre_order(TreeNode *root)\n{\n    if (root == NULL){\n        printf(\"# \");\n        return;\n    }\n    printf(\"%c \", root->val );\n\n    printTree_pre_order(root->left);\n    printTree_pre_order(root->right);\n}\n\nvoid printTree_in_order(TreeNode *root)\n{\n    if (root == NULL){\n        printf(\"# \");\n        return;\n    }\n\n    printTree_in_order(root->left);\n    printf(\"%c \", root->val );\n    printTree_in_order(root->right);\n}\n\n\nvoid printTree_level_order(TreeNode *root)\n{\n    queue<TreeNode*> q;\n    q.push(root);\n    while (q.size()>0){\n        TreeNode* n = q.front();\n        q.pop();\n        if (n==NULL){\n            printf(\"# \");\n            continue;\n        }\n        printf(\"%c \", n->val);\n        q.push(n->left);\n        q.push(n->right);\n    }\n    printf(\"\\n\");\n}\n\n\nint main()\n{\n    int pre_order[]={'F', 'B', 'A', 'D', 'C', 'E', 'G', 'I', 'H'};\n    int  in_order[]={'A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I'};\n    vector<int> preorder( pre_order, pre_order + 9 );\n    vector<int>  inorder(  in_order,  in_order + 9 );\n\n    TreeNode* tree = buildTree(preorder, inorder);\n\n    printTree_level_order(tree);\n    printTree_pre_order(tree);\n    printf(\"\\n\");\n    printTree_in_order(tree);\n    printf(\"\\n\");\n    \n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return 0;\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 15,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 30,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 44,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 45,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 133,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    },
    {
      "id": "java",
      "label": "Java",
      "sourceCount": 2,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-java-0105-construct-binary-tree-from-preorder-and-inorder-traversal.java",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/java/0105-construct-binary-tree-from-preorder-and-inorder-traversal.java",
          "localPath": "neetcode-gh_leetcode/java/0105-construct-binary-tree-from-preorder-and-inorder-traversal.java",
          "relativePath": "java/0105-construct-binary-tree-from-preorder-and-inorder-traversal.java",
          "languageId": "java",
          "languageLabel": "Java",
          "coverageType": "code",
          "code": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     int val;\n *     TreeNode left;\n *     TreeNode right;\n *     TreeNode() {}\n *     TreeNode(int val) { this.val = val; }\n *     TreeNode(int val, TreeNode left, TreeNode right) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\nclass Solution {\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        if (preorder.length == 0 || inorder.length == 0) return null;\n\n        TreeNode root = new TreeNode(preorder[0]);\n        int mid = 0;\n        for (int i = 0; i < inorder.length; i++) {\n            if (preorder[0] == inorder[i]) mid = i;\n        }\n\n        root.left =\n            buildTree(\n                Arrays.copyOfRange(preorder, 1, mid + 1),\n                Arrays.copyOfRange(inorder, 0, mid)\n            );\n        root.right =\n            buildTree(\n                Arrays.copyOfRange(preorder, mid + 1, preorder.length),\n                Arrays.copyOfRange(inorder, mid + 1, inorder.length)\n            );\n\n        return root;\n    }\n}\n\n// Solution without using Array copies\nclass Solution {\n\n    Map<Integer, Integer> inorderPositions = new HashMap<>();\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        if (preorder.length < 1 || inorder.length < 1) return null;\n\n        for (int i = 0; i < inorder.length; i++) {\n            inorderPositions.put(inorder[i], i);\n        }\n\n        return builder(preorder, 0, 0, inorder.length - 1);\n    }\n\n    public TreeNode builder(\n        int[] preorder,\n        int preorderIndex,\n        int inorderLow,\n        int inorderHigh\n    ) {\n        if (\n            preorderIndex > preorder.length - 1 || inorderLow > inorderHigh\n        ) return null;\n\n        int currentVal = preorder[preorderIndex];\n        TreeNode n = new TreeNode(currentVal);\n        int mid = inorderPositions.get(currentVal);\n\n        n.left = builder(preorder, preorderIndex + 1, inorderLow, mid - 1);\n        n.right =\n            builder(\n                preorder,\n                preorderIndex + (mid - inorderLow) + 1,\n                mid + 1,\n                inorderHigh\n            );\n\n        return n;\n    }\n}\n",
          "originalCode": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     int val;\n *     TreeNode left;\n *     TreeNode right;\n *     TreeNode() {}\n *     TreeNode(int val) { this.val = val; }\n *     TreeNode(int val, TreeNode left, TreeNode right) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\nclass Solution {\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        if (preorder.length == 0 || inorder.length == 0) return null;\n\n        TreeNode root = new TreeNode(preorder[0]);\n        int mid = 0;\n        for (int i = 0; i < inorder.length; i++) {\n            if (preorder[0] == inorder[i]) mid = i;\n        }\n\n        root.left =\n            buildTree(\n                Arrays.copyOfRange(preorder, 1, mid + 1),\n                Arrays.copyOfRange(inorder, 0, mid)\n            );\n        root.right =\n            buildTree(\n                Arrays.copyOfRange(preorder, mid + 1, preorder.length),\n                Arrays.copyOfRange(inorder, mid + 1, inorder.length)\n            );\n\n        return root;\n    }\n}\n\n// Solution without using Array copies\nclass Solution {\n\n    Map<Integer, Integer> inorderPositions = new HashMap<>();\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        if (preorder.length < 1 || inorder.length < 1) return null;\n\n        for (int i = 0; i < inorder.length; i++) {\n            inorderPositions.put(inorder[i], i);\n        }\n\n        return builder(preorder, 0, 0, inorder.length - 1);\n    }\n\n    public TreeNode builder(\n        int[] preorder,\n        int preorderIndex,\n        int inorderLow,\n        int inorderHigh\n    ) {\n        if (\n            preorderIndex > preorder.length - 1 || inorderLow > inorderHigh\n        ) return null;\n\n        int currentVal = preorder[preorderIndex];\n        TreeNode n = new TreeNode(currentVal);\n        int mid = inorderPositions.get(currentVal);\n\n        n.left = builder(preorder, preorderIndex + 1, inorderLow, mid - 1);\n        n.right =\n            builder(\n                preorder,\n                preorderIndex + (mid - inorderLow) + 1,\n                mid + 1,\n                inorderHigh\n            );\n\n        return n;\n    }\n}\n",
          "reviewedCode": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     int val;\n *     TreeNode left;\n *     TreeNode right;\n *     TreeNode() {}\n *     TreeNode(int val) { this.val = val; }\n *     TreeNode(int val, TreeNode left, TreeNode right) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nclass Solution {\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        if (preorder.length == 0 || inorder.length == 0) return null;\n\n        TreeNode root = new TreeNode(preorder[0]);\n        int mid = 0;\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (int i = 0; i < inorder.length; i++) {\n            // [Reader note] Decision point\n            // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n            if (preorder[0] == inorder[i]) mid = i;\n        }\n\n        root.left =\n            buildTree(\n                Arrays.copyOfRange(preorder, 1, mid + 1),\n                Arrays.copyOfRange(inorder, 0, mid)\n            );\n        root.right =\n            buildTree(\n                Arrays.copyOfRange(preorder, mid + 1, preorder.length),\n                Arrays.copyOfRange(inorder, mid + 1, inorder.length)\n            );\n\n        return root;\n    }\n}\n\n// Solution without using Array copies\nclass Solution {\n\n    Map<Integer, Integer> inorderPositions = new HashMap<>();\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        if (preorder.length < 1 || inorder.length < 1) return null;\n\n        for (int i = 0; i < inorder.length; i++) {\n            inorderPositions.put(inorder[i], i);\n        }\n\n        return builder(preorder, 0, 0, inorder.length - 1);\n    }\n\n    public TreeNode builder(\n        int[] preorder,\n        int preorderIndex,\n        int inorderLow,\n        int inorderHigh\n    ) {\n        if (\n            preorderIndex > preorder.length - 1 || inorderLow > inorderHigh\n        ) return null;\n\n        int currentVal = preorder[preorderIndex];\n        TreeNode n = new TreeNode(currentVal);\n        int mid = inorderPositions.get(currentVal);\n\n        n.left = builder(preorder, preorderIndex + 1, inorderLow, mid - 1);\n        n.right =\n            builder(\n                preorder,\n                preorderIndex + (mid - inorderLow) + 1,\n                mid + 1,\n                inorderHigh\n            );\n\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return n;\n    }\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 16,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 19,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 23,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 24,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 80,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "doocs_leetcode-solution-0100-0199-0105.construct-binary-tree-from-preorder-and-inorder-traversal-solution.java",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/Solution.java",
          "localPath": "doocs_leetcode/solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.java",
          "relativePath": "solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.java",
          "languageId": "java",
          "languageLabel": "Java",
          "coverageType": "code+explanation",
          "code": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     int val;\n *     TreeNode left;\n *     TreeNode right;\n *     TreeNode() {}\n *     TreeNode(int val) { this.val = val; }\n *     TreeNode(int val, TreeNode left, TreeNode right) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\nclass Solution {\n    private int[] preorder;\n    private Map<Integer, Integer> d = new HashMap<>();\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        int n = preorder.length;\n        this.preorder = preorder;\n        for (int i = 0; i < n; ++i) {\n            d.put(inorder[i], i);\n        }\n        return dfs(0, 0, n);\n    }\n\n    private TreeNode dfs(int i, int j, int n) {\n        if (n <= 0) {\n            return null;\n        }\n        int v = preorder[i];\n        int k = d.get(v);\n        TreeNode l = dfs(i + 1, j, k - j);\n        TreeNode r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n        return new TreeNode(v, l, r);\n    }\n}",
          "originalCode": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     int val;\n *     TreeNode left;\n *     TreeNode right;\n *     TreeNode() {}\n *     TreeNode(int val) { this.val = val; }\n *     TreeNode(int val, TreeNode left, TreeNode right) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\nclass Solution {\n    private int[] preorder;\n    private Map<Integer, Integer> d = new HashMap<>();\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        int n = preorder.length;\n        this.preorder = preorder;\n        for (int i = 0; i < n; ++i) {\n            d.put(inorder[i], i);\n        }\n        return dfs(0, 0, n);\n    }\n\n    private TreeNode dfs(int i, int j, int n) {\n        if (n <= 0) {\n            return null;\n        }\n        int v = preorder[i];\n        int k = d.get(v);\n        TreeNode l = dfs(i + 1, j, k - j);\n        TreeNode r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n        return new TreeNode(v, l, r);\n    }\n}",
          "reviewedCode": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     int val;\n *     TreeNode left;\n *     TreeNode right;\n *     TreeNode() {}\n *     TreeNode(int val) { this.val = val; }\n *     TreeNode(int val, TreeNode left, TreeNode right) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nclass Solution {\n    private int[] preorder;\n    // [Reader note] State setup\n    // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n    private Map<Integer, Integer> d = new HashMap<>();\n\n    public TreeNode buildTree(int[] preorder, int[] inorder) {\n        int n = preorder.length;\n        this.preorder = preorder;\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (int i = 0; i < n; ++i) {\n            d.put(inorder[i], i);\n        }\n        return dfs(0, 0, n);\n    }\n\n    private TreeNode dfs(int i, int j, int n) {\n        // [Reader note] Decision point\n        // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if (n <= 0) {\n            return null;\n        }\n        int v = preorder[i];\n        int k = d.get(v);\n        TreeNode l = dfs(i + 1, j, k - j);\n        TreeNode r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return new TreeNode(v, l, r);\n    }\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 16,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 18,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 23,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 30,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 37,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    },
    {
      "id": "go",
      "label": "Go",
      "sourceCount": 2,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-go-0105-construct-binary-tree-from-preorder-and-inorder-traversal.go",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/go/0105-construct-binary-tree-from-preorder-and-inorder-traversal.go",
          "localPath": "neetcode-gh_leetcode/go/0105-construct-binary-tree-from-preorder-and-inorder-traversal.go",
          "relativePath": "go/0105-construct-binary-tree-from-preorder-and-inorder-traversal.go",
          "languageId": "go",
          "languageLabel": "Go",
          "coverageType": "code",
          "code": "func buildTree(preorder []int, inorder []int) *TreeNode {\n    if len(preorder) == 0 || len(inorder) == 0 {\n        return nil\n    }\n    \n    root := &TreeNode{Val: preorder[0]}\n    mid := index(inorder, preorder[0])\n    root.Left = buildTree(preorder[1: mid + 1], inorder[:mid])\n    root.Right = buildTree(preorder[mid + 1:], inorder[mid + 1:])\n    return root\n}\n\nfunc index(arr []int, val int) int {\n    for i, v := range arr {\n        if v == val {\n            return i\n        }\n    }\n    return -1\n}\n",
          "originalCode": "func buildTree(preorder []int, inorder []int) *TreeNode {\n    if len(preorder) == 0 || len(inorder) == 0 {\n        return nil\n    }\n    \n    root := &TreeNode{Val: preorder[0]}\n    mid := index(inorder, preorder[0])\n    root.Left = buildTree(preorder[1: mid + 1], inorder[:mid])\n    root.Right = buildTree(preorder[mid + 1:], inorder[mid + 1:])\n    return root\n}\n\nfunc index(arr []int, val int) int {\n    for i, v := range arr {\n        if v == val {\n            return i\n        }\n    }\n    return -1\n}\n",
          "reviewedCode": "// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nfunc buildTree(preorder []int, inorder []int) *TreeNode {\n    if len(preorder) == 0 || len(inorder) == 0 {\n        return nil\n    }\n    \n    // [Reader note] State setup\n    // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n    root := &TreeNode{Val: preorder[0]}\n    mid := index(inorder, preorder[0])\n    root.Left = buildTree(preorder[1: mid + 1], inorder[:mid])\n    root.Right = buildTree(preorder[mid + 1:], inorder[mid + 1:])\n    return root\n}\n\nfunc index(arr []int, val int) int {\n    // [Reader note] Main pass\n    // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n    for i, v := range arr {\n        // [Reader note] Decision point\n        // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if v == val {\n            return i\n        }\n    }\n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return -1\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 6,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 14,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 15,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 19,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        },
        {
          "id": "doocs_leetcode-solution-0100-0199-0105.construct-binary-tree-from-preorder-and-inorder-traversal-solution.go",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/Solution.go",
          "localPath": "doocs_leetcode/solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.go",
          "relativePath": "solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.go",
          "languageId": "go",
          "languageLabel": "Go",
          "coverageType": "code+explanation",
          "code": "/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc buildTree(preorder []int, inorder []int) *TreeNode {\n\td := map[int]int{}\n\tfor i, x := range inorder {\n\t\td[x] = i\n\t}\n\tvar dfs func(i, j, n int) *TreeNode\n\tdfs = func(i, j, n int) *TreeNode {\n\t\tif n <= 0 {\n\t\t\treturn nil\n\t\t}\n\t\tv := preorder[i]\n\t\tk := d[v]\n\t\tl := dfs(i+1, j, k-j)\n\t\tr := dfs(i+1+k-j, k+1, n-1-(k-j))\n\t\treturn &TreeNode{v, l, r}\n\t}\n\treturn dfs(0, 0, len(preorder))\n}",
          "originalCode": "/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc buildTree(preorder []int, inorder []int) *TreeNode {\n\td := map[int]int{}\n\tfor i, x := range inorder {\n\t\td[x] = i\n\t}\n\tvar dfs func(i, j, n int) *TreeNode\n\tdfs = func(i, j, n int) *TreeNode {\n\t\tif n <= 0 {\n\t\t\treturn nil\n\t\t}\n\t\tv := preorder[i]\n\t\tk := d[v]\n\t\tl := dfs(i+1, j, k-j)\n\t\tr := dfs(i+1+k-j, k+1, n-1-(k-j))\n\t\treturn &TreeNode{v, l, r}\n\t}\n\treturn dfs(0, 0, len(preorder))\n}",
          "reviewedCode": "/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nfunc buildTree(preorder []int, inorder []int) *TreeNode {\n\t// [Reader note] State setup\n\t// This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n\td := map[int]int{}\n\t// [Reader note] Main pass\n\t// This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n\tfor i, x := range inorder {\n\t\td[x] = i\n\t}\n\tvar dfs func(i, j, n int) *TreeNode\n\tdfs = func(i, j, n int) *TreeNode {\n\t\t// [Reader note] Decision point\n\t\t// Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n\t\tif n <= 0 {\n\t\t\treturn nil\n\t\t}\n\t\tv := preorder[i]\n\t\tk := d[v]\n\t\tl := dfs(i+1, j, k-j)\n\t\tr := dfs(i+1+k-j, k+1, n-1-(k-j))\n\t\treturn &TreeNode{v, l, r}\n\t}\n\t// [Reader note] Return\n\t// The final return exposes the result after the traversal and state updates have settled.\n\treturn dfs(0, 0, len(preorder))\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 9,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 10,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 11,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 16,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 25,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    },
    {
      "id": "typescript",
      "label": "TypeScript",
      "sourceCount": 1,
      "sources": [
        {
          "id": "doocs_leetcode-solution-0100-0199-0105.construct-binary-tree-from-preorder-and-inorder-traversal-solution.ts",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/Solution.ts",
          "localPath": "doocs_leetcode/solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.ts",
          "relativePath": "solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/Solution.ts",
          "languageId": "typescript",
          "languageLabel": "TypeScript",
          "coverageType": "code+explanation",
          "code": "/**\n * Definition for a binary tree node.\n * class TreeNode {\n *     val: number\n *     left: TreeNode | null\n *     right: TreeNode | null\n *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {\n *         this.val = (val===undefined ? 0 : val)\n *         this.left = (left===undefined ? null : left)\n *         this.right = (right===undefined ? null : right)\n *     }\n * }\n */\n\nfunction buildTree(preorder: number[], inorder: number[]): TreeNode | null {\n    const d: Map<number, number> = new Map();\n    const n = inorder.length;\n    for (let i = 0; i < n; ++i) {\n        d.set(inorder[i], i);\n    }\n    const dfs = (i: number, j: number, n: number): TreeNode | null => {\n        if (n <= 0) {\n            return null;\n        }\n        const v = preorder[i];\n        const k = d.get(v)!;\n        const l = dfs(i + 1, j, k - j);\n        const r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n        return new TreeNode(v, l, r);\n    };\n    return dfs(0, 0, n);\n}\n",
          "originalCode": "/**\n * Definition for a binary tree node.\n * class TreeNode {\n *     val: number\n *     left: TreeNode | null\n *     right: TreeNode | null\n *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {\n *         this.val = (val===undefined ? 0 : val)\n *         this.left = (left===undefined ? null : left)\n *         this.right = (right===undefined ? null : right)\n *     }\n * }\n */\n\nfunction buildTree(preorder: number[], inorder: number[]): TreeNode | null {\n    const d: Map<number, number> = new Map();\n    const n = inorder.length;\n    for (let i = 0; i < n; ++i) {\n        d.set(inorder[i], i);\n    }\n    const dfs = (i: number, j: number, n: number): TreeNode | null => {\n        if (n <= 0) {\n            return null;\n        }\n        const v = preorder[i];\n        const k = d.get(v)!;\n        const l = dfs(i + 1, j, k - j);\n        const r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n        return new TreeNode(v, l, r);\n    };\n    return dfs(0, 0, n);\n}\n",
          "reviewedCode": "/**\n * Definition for a binary tree node.\n * class TreeNode {\n *     val: number\n *     left: TreeNode | null\n *     right: TreeNode | null\n *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {\n *         this.val = (val===undefined ? 0 : val)\n *         this.left = (left===undefined ? null : left)\n *         this.right = (right===undefined ? null : right)\n *     }\n * }\n */\n\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nfunction buildTree(preorder: number[], inorder: number[]): TreeNode | null {\n    // [Reader note] State setup\n    // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n    const d: Map<number, number> = new Map();\n    const n = inorder.length;\n    // [Reader note] Main pass\n    // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n    for (let i = 0; i < n; ++i) {\n        d.set(inorder[i], i);\n    }\n    const dfs = (i: number, j: number, n: number): TreeNode | null => {\n        // [Reader note] Decision point\n        // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if (n <= 0) {\n            return null;\n        }\n        const v = preorder[i];\n        const k = d.get(v)!;\n        const l = dfs(i + 1, j, k - j);\n        const r = dfs(i + 1 + k - j, k + 1, n - 1 - (k - j));\n        return new TreeNode(v, l, r);\n    };\n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return dfs(0, 0, n);\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 15,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 16,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 18,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 22,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 31,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    },
    {
      "id": "javascript",
      "label": "JavaScript",
      "sourceCount": 1,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-javascript-0105-construct-binary-tree-from-preorder-and-inorder-traversal.js",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/javascript/0105-construct-binary-tree-from-preorder-and-inorder-traversal.js",
          "localPath": "neetcode-gh_leetcode/javascript/0105-construct-binary-tree-from-preorder-and-inorder-traversal.js",
          "relativePath": "javascript/0105-construct-binary-tree-from-preorder-and-inorder-traversal.js",
          "languageId": "javascript",
          "languageLabel": "JavaScript",
          "coverageType": "code",
          "code": "/**\n * https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n * Time O(N^2) | Space(H)\n * @param {number[]} preorder\n * @param {number[]} inorder\n * @return {TreeNode}\n */\nvar buildTree = function (preorder, inorder) {\n    const isBaseCase = !preorder.length || !inorder.length;\n    if (isBaseCase) return null;\n\n    return dfs(preorder, inorder);\n};\n\nvar dfs = (preorder, inorder) => {\n    const { leftInorder, mid, rightInorder } = getPointers(preorder, inorder);\n    const root = new TreeNode(inorder[mid]);\n\n    root.left = buildTree(preorder, leftInorder);\n    root.right = buildTree(preorder, rightInorder);\n\n    return root;\n};\n\nconst getPointers = (preorder, inorder) => {\n    const next = preorder.shift();\n    const mid = inorder.indexOf(next);\n    const leftInorder = inorder.slice(0, mid);\n    const rightInorder = inorder.slice(mid + 1);\n\n    return { leftInorder, mid, rightInorder };\n};\n\n/**\n * https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n * Time O(N) | Space(H)\n * @param {number[]} preorder\n * @param {number[]} inorder\n * @return {TreeNode}\n */\nvar buildTree = function (\n    preorder,\n    inorder,\n    max = -Infinity,\n    indices = { preorder: 0, inorder: 0 },\n) {\n    const isBaseCase = preorder.length <= indices.inorder;\n    if (isBaseCase) return null;\n\n    const isAtEnd = inorder[indices.inorder] === max;\n    if (isAtEnd) {\n        indices.inorder++;\n        return null;\n    }\n\n    return dfs(preorder, inorder, max, indices);\n};\n\nvar dfs = (preorder, inorder, max, indices) => {\n    const val = preorder[indices.preorder++];\n    const root = new TreeNode(val);\n\n    root.left = buildTree(preorder, inorder, root.val, indices);\n    root.right = buildTree(preorder, inorder, max, indices);\n\n    return root;\n};\n",
          "originalCode": "/**\n * https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n * Time O(N^2) | Space(H)\n * @param {number[]} preorder\n * @param {number[]} inorder\n * @return {TreeNode}\n */\nvar buildTree = function (preorder, inorder) {\n    const isBaseCase = !preorder.length || !inorder.length;\n    if (isBaseCase) return null;\n\n    return dfs(preorder, inorder);\n};\n\nvar dfs = (preorder, inorder) => {\n    const { leftInorder, mid, rightInorder } = getPointers(preorder, inorder);\n    const root = new TreeNode(inorder[mid]);\n\n    root.left = buildTree(preorder, leftInorder);\n    root.right = buildTree(preorder, rightInorder);\n\n    return root;\n};\n\nconst getPointers = (preorder, inorder) => {\n    const next = preorder.shift();\n    const mid = inorder.indexOf(next);\n    const leftInorder = inorder.slice(0, mid);\n    const rightInorder = inorder.slice(mid + 1);\n\n    return { leftInorder, mid, rightInorder };\n};\n\n/**\n * https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n * Time O(N) | Space(H)\n * @param {number[]} preorder\n * @param {number[]} inorder\n * @return {TreeNode}\n */\nvar buildTree = function (\n    preorder,\n    inorder,\n    max = -Infinity,\n    indices = { preorder: 0, inorder: 0 },\n) {\n    const isBaseCase = preorder.length <= indices.inorder;\n    if (isBaseCase) return null;\n\n    const isAtEnd = inorder[indices.inorder] === max;\n    if (isAtEnd) {\n        indices.inorder++;\n        return null;\n    }\n\n    return dfs(preorder, inorder, max, indices);\n};\n\nvar dfs = (preorder, inorder, max, indices) => {\n    const val = preorder[indices.preorder++];\n    const root = new TreeNode(val);\n\n    root.left = buildTree(preorder, inorder, root.val, indices);\n    root.right = buildTree(preorder, inorder, max, indices);\n\n    return root;\n};\n",
          "reviewedCode": "/**\n * https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n * Time O(N^2) | Space(H)\n * @param {number[]} preorder\n * @param {number[]} inorder\n * @return {TreeNode}\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nvar buildTree = function (preorder, inorder) {\n    // [Reader note] State setup\n    // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n    const isBaseCase = !preorder.length || !inorder.length;\n    // [Reader note] Decision point\n    // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n    if (isBaseCase) return null;\n\n    return dfs(preorder, inorder);\n};\n\nvar dfs = (preorder, inorder) => {\n    const { leftInorder, mid, rightInorder } = getPointers(preorder, inorder);\n    const root = new TreeNode(inorder[mid]);\n\n    root.left = buildTree(preorder, leftInorder);\n    root.right = buildTree(preorder, rightInorder);\n\n    return root;\n};\n\nconst getPointers = (preorder, inorder) => {\n    const next = preorder.shift();\n    const mid = inorder.indexOf(next);\n    const leftInorder = inorder.slice(0, mid);\n    const rightInorder = inorder.slice(mid + 1);\n\n    return { leftInorder, mid, rightInorder };\n};\n\n/**\n * https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/\n * Time O(N) | Space(H)\n * @param {number[]} preorder\n * @param {number[]} inorder\n * @return {TreeNode}\n */\nvar buildTree = function (\n    preorder,\n    inorder,\n    max = -Infinity,\n    indices = { preorder: 0, inorder: 0 },\n) {\n    const isBaseCase = preorder.length <= indices.inorder;\n    if (isBaseCase) return null;\n\n    const isAtEnd = inorder[indices.inorder] === max;\n    if (isAtEnd) {\n        indices.inorder++;\n        return null;\n    }\n\n    return dfs(preorder, inorder, max, indices);\n};\n\nvar dfs = (preorder, inorder, max, indices) => {\n    const val = preorder[indices.preorder++];\n    const root = new TreeNode(val);\n\n    root.left = buildTree(preorder, inorder, root.val, indices);\n    root.right = buildTree(preorder, inorder, max, indices);\n\n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return root;\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [
            "Time O(N^2) | Space(H)"
          ],
          "annotations": [
            {
              "line": 8,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 9,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 10,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 66,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    },
    {
      "id": "csharp",
      "label": "C#",
      "sourceCount": 1,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-csharp-0105-construct-binary-tree-from-preorder-and-inorder-traversal.cs",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/csharp/0105-construct-binary-tree-from-preorder-and-inorder-traversal.cs",
          "localPath": "neetcode-gh_leetcode/csharp/0105-construct-binary-tree-from-preorder-and-inorder-traversal.cs",
          "relativePath": "csharp/0105-construct-binary-tree-from-preorder-and-inorder-traversal.cs",
          "languageId": "csharp",
          "languageLabel": "C#",
          "coverageType": "code",
          "code": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     public int val;\n *     public TreeNode left;\n *     public TreeNode right;\n *     public TreeNode(int val=0, TreeNode left=null, TreeNode right=null) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\n\npublic class Solution\n{\n    public TreeNode BuildTree(int[] preorder, int[] inorder)\n    {\n        return BuildTreeHelper(0, 0, inorder.Length - 1, preorder, inorder);\n    }\n\n    private TreeNode BuildTreeHelper(int preStart, int inStart, int inEnd, int[] preorder, int[] inorder)\n    {\n        if (preorder.Length == 0 && inorder.Length == 0)\n            return null;\n\n        if (preStart > preorder.Length - 1 || inStart > inEnd)\n            return null;\n\n        var rootNode = new TreeNode(preorder[preStart]);\n        var mid = Array.IndexOf(inorder, preorder[preStart]);\n\n        rootNode.left = BuildTreeHelper(preStart + 1, inStart, mid - 1, preorder, inorder);\n        rootNode.right = BuildTreeHelper(preStart + mid - inStart + 1, mid + 1, inEnd, preorder, inorder);\n\n        return rootNode;\n    }\n}",
          "originalCode": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     public int val;\n *     public TreeNode left;\n *     public TreeNode right;\n *     public TreeNode(int val=0, TreeNode left=null, TreeNode right=null) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\n\npublic class Solution\n{\n    public TreeNode BuildTree(int[] preorder, int[] inorder)\n    {\n        return BuildTreeHelper(0, 0, inorder.Length - 1, preorder, inorder);\n    }\n\n    private TreeNode BuildTreeHelper(int preStart, int inStart, int inEnd, int[] preorder, int[] inorder)\n    {\n        if (preorder.Length == 0 && inorder.Length == 0)\n            return null;\n\n        if (preStart > preorder.Length - 1 || inStart > inEnd)\n            return null;\n\n        var rootNode = new TreeNode(preorder[preStart]);\n        var mid = Array.IndexOf(inorder, preorder[preStart]);\n\n        rootNode.left = BuildTreeHelper(preStart + 1, inStart, mid - 1, preorder, inorder);\n        rootNode.right = BuildTreeHelper(preStart + mid - inStart + 1, mid + 1, inEnd, preorder, inorder);\n\n        return rootNode;\n    }\n}",
          "reviewedCode": "/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     public int val;\n *     public TreeNode left;\n *     public TreeNode right;\n // [Reader note] Entry point\n // Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\n *     public TreeNode(int val=0, TreeNode left=null, TreeNode right=null) {\n // [Reader note] State setup\n // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\n\npublic class Solution\n{\n    public TreeNode BuildTree(int[] preorder, int[] inorder)\n    {\n        return BuildTreeHelper(0, 0, inorder.Length - 1, preorder, inorder);\n    }\n\n    private TreeNode BuildTreeHelper(int preStart, int inStart, int inEnd, int[] preorder, int[] inorder)\n    {\n        // [Reader note] Decision point\n        // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if (preorder.Length == 0 && inorder.Length == 0)\n            return null;\n\n        if (preStart > preorder.Length - 1 || inStart > inEnd)\n            return null;\n\n        var rootNode = new TreeNode(preorder[preStart]);\n        var mid = Array.IndexOf(inorder, preorder[preStart]);\n\n        rootNode.left = BuildTreeHelper(preStart + 1, inStart, mid - 1, preorder, inorder);\n        rootNode.right = BuildTreeHelper(preStart + mid - inStart + 1, mid + 1, inEnd, preorder, inorder);\n\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return rootNode;\n    }\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 7,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 8,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 24,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 36,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    },
    {
      "id": "c",
      "label": "C",
      "sourceCount": 1,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-c-0105-construct-binary-tree-from-preorder-and-inorder-traversal.c",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/c/0105-construct-binary-tree-from-preorder-and-inorder-traversal.c",
          "localPath": "neetcode-gh_leetcode/c/0105-construct-binary-tree-from-preorder-and-inorder-traversal.c",
          "relativePath": "c/0105-construct-binary-tree-from-preorder-and-inorder-traversal.c",
          "languageId": "c",
          "languageLabel": "C",
          "coverageType": "code",
          "code": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     struct TreeNode *left;\n *     struct TreeNode *right;\n * };\n */\ntypedef struct TreeNode TreeNode;\n\nTreeNode* build(int* preorder, int* inorder, int* rootIndex, int inorderLeft, int inorderRight)\n{\n    if(inorderLeft >= inorderRight)\n    {\n        return NULL;\n    }\n\n    TreeNode* root = (TreeNode*)malloc(sizeof(TreeNode));\n    root->val = preorder[*rootIndex];\n    int mid = 0;\n\n    // Finding mid\n    for(size_t i = inorderLeft; i < inorderRight; i++)\n    {\n        if(inorder[i] == preorder[*rootIndex])\n        {\n            mid = i;\n            break;\n        }\n    }\n\n    (*rootIndex)++;\n\n    root->left = build(preorder, inorder, rootIndex, inorderLeft, mid);\n    root->right = build(preorder, inorder, rootIndex, mid + 1, inorderRight);\n\n    return root;\n}\n\nstruct TreeNode* buildTree(int* preorder, int preorderSize, int* inorder, int inorderSize){\n    int rootIndex = 0;\n    return build(preorder, inorder, &rootIndex, 0, inorderSize);\n}\n",
          "originalCode": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     struct TreeNode *left;\n *     struct TreeNode *right;\n * };\n */\ntypedef struct TreeNode TreeNode;\n\nTreeNode* build(int* preorder, int* inorder, int* rootIndex, int inorderLeft, int inorderRight)\n{\n    if(inorderLeft >= inorderRight)\n    {\n        return NULL;\n    }\n\n    TreeNode* root = (TreeNode*)malloc(sizeof(TreeNode));\n    root->val = preorder[*rootIndex];\n    int mid = 0;\n\n    // Finding mid\n    for(size_t i = inorderLeft; i < inorderRight; i++)\n    {\n        if(inorder[i] == preorder[*rootIndex])\n        {\n            mid = i;\n            break;\n        }\n    }\n\n    (*rootIndex)++;\n\n    root->left = build(preorder, inorder, rootIndex, inorderLeft, mid);\n    root->right = build(preorder, inorder, rootIndex, mid + 1, inorderRight);\n\n    return root;\n}\n\nstruct TreeNode* buildTree(int* preorder, int preorderSize, int* inorder, int inorderSize){\n    int rootIndex = 0;\n    return build(preorder, inorder, &rootIndex, 0, inorderSize);\n}\n",
          "reviewedCode": "/**\n * Definition for a binary tree node.\n * struct TreeNode {\n *     int val;\n *     struct TreeNode *left;\n *     struct TreeNode *right;\n * };\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\ntypedef struct TreeNode TreeNode;\n\nTreeNode* build(int* preorder, int* inorder, int* rootIndex, int inorderLeft, int inorderRight)\n{\n    if(inorderLeft >= inorderRight)\n    {\n        return NULL;\n    }\n\n    // [Reader note] State setup\n    // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n    TreeNode* root = (TreeNode*)malloc(sizeof(TreeNode));\n    root->val = preorder[*rootIndex];\n    int mid = 0;\n\n    // Finding mid\n    // [Reader note] Main pass\n    // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n    for(size_t i = inorderLeft; i < inorderRight; i++)\n    {\n        // [Reader note] Decision point\n        // Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise.\n        if(inorder[i] == preorder[*rootIndex])\n        {\n            mid = i;\n            break;\n        }\n    }\n\n    (*rootIndex)++;\n\n    root->left = build(preorder, inorder, rootIndex, inorderLeft, mid);\n    root->right = build(preorder, inorder, rootIndex, mid + 1, inorderRight);\n\n    return root;\n}\n\nstruct TreeNode* buildTree(int* preorder, int preorderSize, int* inorder, int inorderSize){\n    int rootIndex = 0;\n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return build(preorder, inorder, &rootIndex, 0, inorderSize);\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 9,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 18,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 23,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 25,
              "label": "Decision point",
              "note": "Pay attention here: The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
            },
            {
              "line": 42,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    },
    {
      "id": "kotlin",
      "label": "Kotlin",
      "sourceCount": 1,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-kotlin-0105-construct-binary-tree-from-preorder-and-inorder-traversal.kt",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/kotlin/0105-construct-binary-tree-from-preorder-and-inorder-traversal.kt",
          "localPath": "neetcode-gh_leetcode/kotlin/0105-construct-binary-tree-from-preorder-and-inorder-traversal.kt",
          "relativePath": "kotlin/0105-construct-binary-tree-from-preorder-and-inorder-traversal.kt",
          "languageId": "kotlin",
          "languageLabel": "Kotlin",
          "coverageType": "code",
          "code": "/**\n * Example:\n * var ti = TreeNode(5)\n * var v = ti.`val`\n * Definition for a binary tree node.\n * class TreeNode(var `val`: Int) {\n *     var left: TreeNode? = null\n *     var right: TreeNode? = null\n * }\n */\nclass Solution {\n    fun buildTree(preorder: IntArray, inorder: IntArray): TreeNode? {\n        val prStart = 0; val inStart = 0\n        val prEnd = preorder.size-1; val inEnd = inorder.size-1\n\n        return helper(preorder, prStart, prEnd, inorder, inStart, inEnd);\n    }\n\n    fun helper(preorder: IntArray, prStart: Int, prEnd: Int, inorder: IntArray, inStart: Int, inEnd: Int): TreeNode? {\n        if (prStart > prEnd || inStart > inEnd)\n            return null\n\n        val root = TreeNode(preorder[prStart])\n        var i = 0\n\n        while (i < inorder.size && inorder[i] != preorder[prStart]) {\n            i++\n        }\n\n        root.left = helper(preorder, prStart+1, prStart-inStart+i, inorder, inStart, i-1)\n        root.right = helper(preorder, prStart-inStart+i+1, prEnd, inorder, i+1, inEnd)\n\n        return root\n    }\n}",
          "originalCode": "/**\n * Example:\n * var ti = TreeNode(5)\n * var v = ti.`val`\n * Definition for a binary tree node.\n * class TreeNode(var `val`: Int) {\n *     var left: TreeNode? = null\n *     var right: TreeNode? = null\n * }\n */\nclass Solution {\n    fun buildTree(preorder: IntArray, inorder: IntArray): TreeNode? {\n        val prStart = 0; val inStart = 0\n        val prEnd = preorder.size-1; val inEnd = inorder.size-1\n\n        return helper(preorder, prStart, prEnd, inorder, inStart, inEnd);\n    }\n\n    fun helper(preorder: IntArray, prStart: Int, prEnd: Int, inorder: IntArray, inStart: Int, inEnd: Int): TreeNode? {\n        if (prStart > prEnd || inStart > inEnd)\n            return null\n\n        val root = TreeNode(preorder[prStart])\n        var i = 0\n\n        while (i < inorder.size && inorder[i] != preorder[prStart]) {\n            i++\n        }\n\n        root.left = helper(preorder, prStart+1, prStart-inStart+i, inorder, inStart, i-1)\n        root.right = helper(preorder, prStart-inStart+i+1, prEnd, inorder, i+1, inEnd)\n\n        return root\n    }\n}",
          "reviewedCode": "/**\n * Example:\n * var ti = TreeNode(5)\n * var v = ti.`val`\n * Definition for a binary tree node.\n * class TreeNode(var `val`: Int) {\n *     var left: TreeNode? = null\n *     var right: TreeNode? = null\n * }\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions.\nclass Solution {\n    fun buildTree(preorder: IntArray, inorder: IntArray): TreeNode? {\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.\n        val prStart = 0; val inStart = 0\n        val prEnd = preorder.size-1; val inEnd = inorder.size-1\n\n        return helper(preorder, prStart, prEnd, inorder, inStart, inEnd);\n    }\n\n    fun helper(preorder: IntArray, prStart: Int, prEnd: Int, inorder: IntArray, inStart: Int, inEnd: Int): TreeNode? {\n        if (prStart > prEnd || inStart > inEnd)\n            return null\n\n        val root = TreeNode(preorder[prStart])\n        var i = 0\n\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        while (i < inorder.size && inorder[i] != preorder[prStart]) {\n            i++\n        }\n\n        root.left = helper(preorder, prStart+1, prStart-inStart+i, inorder, inStart, i-1)\n        root.right = helper(preorder, prStart-inStart+i+1, prEnd, inorder, i+1, inEnd)\n\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return root\n    }\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 11,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Each traversal tells you something different; combine them to carve the tree into smaller valid regions."
            },
            {
              "line": 13,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices."
            },
            {
              "line": 26,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 33,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It is a very good transition problem for learning how recursive subproblems are defined by ranges instead of copied arrays.",
            "whatToNotice": "Recursive decomposition, boundary indexing, and mapping traversal semantics to subtree slices.",
            "riskPoint": "The statement is clear, but sloppy range bookkeeping quickly turns the recursion into noise."
          }
        }
      ]
    }
  ],
  "writeups": [
    {
      "id": "doocs_leetcode-solution-0100-0199-0105.construct-binary-tree-from-preorder-and-inorder-traversal-readme.md",
      "repository": "doocs_leetcode",
      "repositoryUrl": "https://github.com/doocs/leetcode",
      "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/README.md",
      "localPath": "doocs_leetcode/solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/README.md",
      "relativePath": "solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/README.md",
      "title": "[105. \u4ece\u524d\u5e8f\u4e0e\u4e2d\u5e8f\u904d\u5386\u5e8f\u5217\u6784\u9020\u4e8c\u53c9\u6811](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal)",
      "excerpt": "--- comments: true difficulty: \u4e2d\u7b49 edit_url: https://github.com/doocs/leetcode/edit/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/README.md tags: - \u6811 - \u6570\u7ec4 - \u54c8\u5e0c\u8868 - \u5206\u6cbb - \u4e8c\u53c9\u6811 --- <!-- problem:start --> # [105. \u4ece\u524d\u5e8f\u4e0e\u4e2d\u5e8f\u904d\u5386\u5e8f\u5217\u6784\u9020\u4e8c\u53c9\u6811](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal) [English Version](/solution/0100-0199/0105.Construct%..."
    },
    {
      "id": "doocs_leetcode-solution-0100-0199-0105.construct-binary-tree-from-preorder-and-inorder-traversal-readme_en.md",
      "repository": "doocs_leetcode",
      "repositoryUrl": "https://github.com/doocs/leetcode",
      "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/README_EN.md",
      "localPath": "doocs_leetcode/solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/README_EN.md",
      "relativePath": "solution/0100-0199/0105.Construct Binary Tree from Preorder and Inorder Traversal/README_EN.md",
      "title": "[105. Construct Binary Tree from Preorder and Inorder Traversal](https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal)",
      "excerpt": "--- comments: true difficulty: Medium edit_url: https://github.com/doocs/leetcode/edit/main/solution/0100-0199/0105.Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/README_EN.md tags: - Tree - Array - Hash Table - Divide and Conquer - Binary Tree --- <!-- problem:start --> # [105. Construct Binary Tree from Preorder and Inorder Traversal](https://leetcode.com/problems/construct-binary-tree-fro..."
    },
    {
      "id": "wisdompeak_leetcode-tree-105.construct-binary-tree-from-preorder-and-inorder-traversal-readme.md",
      "repository": "wisdompeak_LeetCode",
      "repositoryUrl": "https://github.com/wisdompeak/LeetCode",
      "sourceUrl": "https://github.com/wisdompeak/LeetCode/blob/master/Tree/105.Construct-Binary-Tree-from-Preorder-and-Inorder-Traversal/Readme.md",
      "localPath": "wisdompeak_LeetCode/Tree/105.Construct-Binary-Tree-from-Preorder-and-Inorder-Traversal/Readme.md",
      "relativePath": "Tree/105.Construct-Binary-Tree-from-Preorder-and-Inorder-Traversal/Readme.md",
      "title": "105.Construct-Binary-Tree-from-Preorder-and-Inorder-Traversal",
      "excerpt": "### 105.Construct-Binary-Tree-from-Preorder-and-Inorder-Traversal \u9996\u5148\u660e\u786e\u6982\u5ff5\uff0c\u5148\u5e8f\u904d\u5386\uff08perorder\uff09\u662f\u6307\u7b2c\u4e00\u4e2a\u8bbf\u95ee\u6839\u8282\u70b9\uff1b\u4e2d\u5e8f\u904d\u5386\uff08inorder\uff09\u662f\u6307\u7b2c\u4e8c\u4e2a\u8bbf\u95ee\u6839\u8282\u70b9\uff08\u7b2c\u4e00\u4e2a\u5148\u8bbf\u95ee\u5de6\u5b50\u6811\uff09\u3002\u4e0d\u8981\u6df7\u6dc6\u4e86\u3002 \u672c\u9898\u662f\u7ecf\u5178\u7684\u4e8c\u53c9\u6811\u64cd\u4f5c\u3002\u8003\u8651\u5230preorder\u7684\u7b2c\u4e00\u4e2a\u5143\u7d20\u5fc5\u5b9a\u662froot\uff1b\u6240\u4ee5\u5728inorder\u4e2d\u627e\u5230\u5bf9\u5e94root\u7684\u4f4d\u7f6e\u540e\uff0c\u5176\u5de6\u8fb9\u5c31\u662f\u5de6\u5b50\u6811\u7684\u6240\u6709\u8282\u70b9\u7684\u4e2d\u5e8f\u904d\u5386\uff0c\u53f3\u8fb9\u5c31\u662f\u53f3\u5b50\u6811\u7684\u6240\u6709\u8282\u70b9\u7684\u4e2d\u5e8f\u904d\u5386\u3002 \u90a3\u4e48\u5982\u4f55\u5728preorder\u5e8f\u5217\u91cc\u9762\u533a\u522b\u51fa\u54ea\u4e9b\u662f\u5de6\u5b50\u6811\u7684\u8282\u70b9\uff0c\u54ea\u4e9b\u662f\u53f3\u5b50\u6811\u7684\u8282\u70b9\u5462\uff1f\u901a\u8fc7inorder\u5e8f\u5217\u91cc\u5de6\u5b50\u6811\u8282\u70b9\u7684\u6570\u76ee\uff01\u5047\u8bbeinorder\u5e8f\u5217\u91cc\u5de6\u5b50\u6811\u8282\u70b9\u7684\u6570\u76ee\u4e3aN\uff0c\u90a3\u4e48\u5728preorder\u5e8f\u5217\u91cc\uff0croot\u4e4b\u540e\u7684N\u4e2a\u5143\u7d20\u5c31\u662f\u5de6\u5b50\u6811\u7684\u5148\u5e8f\u904d\u5386\uff0c\u5269\u4e0b\u7684\u5143\u7d20\u5c31\u662f\u53f3\u5b50\u6811\u7684\u5148\u5e8f\u904d\u5386\u3002 \u6ce8\u610f\uff0c\u9700\u8981\u63d0\u524d\u6839\u636einorder\u5efa\u7acbHash\u8868\uff0c\u8fd9\u6837\u4ecepreorder\u91cc\u786e\u5b9aroo..."
    }
  ]
}
