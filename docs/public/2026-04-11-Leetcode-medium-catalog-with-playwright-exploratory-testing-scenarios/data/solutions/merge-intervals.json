{
  "generatedAt": "2026-04-11",
  "problemId": "merge-intervals",
  "problemTitle": "Merge Intervals",
  "problemNumber": 56,
  "summary": {
    "hasSolutions": true,
    "languageIds": [
      "python",
      "cpp",
      "java",
      "go",
      "typescript",
      "javascript",
      "csharp",
      "c"
    ],
    "languageLabels": [
      "Python",
      "C++",
      "Java",
      "Go",
      "TypeScript",
      "JavaScript",
      "C#",
      "C"
    ],
    "solutionCount": 15,
    "writeupCount": 5,
    "primaryLanguageId": "python"
  },
  "languages": [
    {
      "id": "python",
      "label": "Python",
      "sourceCount": 2,
      "sources": [
        {
          "id": "kamyu104_leetcode-solutions-python-merge-intervals.py",
          "repository": "kamyu104_LeetCode-Solutions",
          "repositoryUrl": "https://github.com/kamyu104/LeetCode-Solutions",
          "sourceUrl": "https://github.com/kamyu104/LeetCode-Solutions/blob/master/Python/merge-intervals.py",
          "localPath": "kamyu104_LeetCode-Solutions/Python/merge-intervals.py",
          "relativePath": "Python/merge-intervals.py",
          "languageId": "python",
          "languageLabel": "Python",
          "coverageType": "indexed-code",
          "code": "# Time:  O(nlogn)\n# Space: O(1)\n\nclass Solution(object):\n    def merge(self, intervals):\n        \"\"\"\n        :type intervals: List[List[int]]\n        :rtype: List[List[int]]\n        \"\"\"\n        intervals.sort()\n        result = []\n        for interval in intervals:\n            if not result or interval[0] > result[-1][1]:\n                result.append(interval)\n            else:\n                result[-1][1] = max(result[-1][1], interval[1])\n        return result\n",
          "originalCode": "# Time:  O(nlogn)\n# Space: O(1)\n\nclass Solution(object):\n    def merge(self, intervals):\n        \"\"\"\n        :type intervals: List[List[int]]\n        :rtype: List[List[int]]\n        \"\"\"\n        intervals.sort()\n        result = []\n        for interval in intervals:\n            if not result or interval[0] > result[-1][1]:\n                result.append(interval)\n            else:\n                result[-1][1] = max(result[-1][1], interval[1])\n        return result\n",
          "reviewedCode": "# Time:  O(nlogn)\n# Space: O(1)\n\n# [Reader note] Entry point\n# Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nclass Solution(object):\n    def merge(self, intervals):\n        \"\"\"\n        :type intervals: List[List[int]]\n        :rtype: List[List[int]]\n        \"\"\"\n        intervals.sort()\n        # [Reader note] State setup\n        # This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        result = []\n        # [Reader note] Main pass\n        # This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for interval in intervals:\n            # [Reader note] Decision point\n            # Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if not result or interval[0] > result[-1][1]:\n                result.append(interval)\n            else:\n                result[-1][1] = max(result[-1][1], interval[1])\n        # [Reader note] Return\n        # The final return exposes the result after the traversal and state updates have settled.\n        return result",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [
            "Time:  O(nlogn)",
            "Space: O(1)"
          ],
          "annotations": [
            {
              "line": 4,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 11,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 12,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 13,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 17,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        },
        {
          "id": "doocs_leetcode-solution-0000-0099-0056.merge-intervals-solution.py",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0056.Merge%20Intervals/Solution.py",
          "localPath": "doocs_leetcode/solution/0000-0099/0056.Merge Intervals/Solution.py",
          "relativePath": "solution/0000-0099/0056.Merge Intervals/Solution.py",
          "languageId": "python",
          "languageLabel": "Python",
          "coverageType": "code+explanation",
          "code": "class Solution:\n    def merge(self, intervals: List[List[int]]) -> List[List[int]]:\n        intervals.sort()\n        ans = []\n        st, ed = intervals[0]\n        for s, e in intervals[1:]:\n            if ed < s:\n                ans.append([st, ed])\n                st, ed = s, e\n            else:\n                ed = max(ed, e)\n        ans.append([st, ed])\n        return ans\n",
          "originalCode": "class Solution:\n    def merge(self, intervals: List[List[int]]) -> List[List[int]]:\n        intervals.sort()\n        ans = []\n        st, ed = intervals[0]\n        for s, e in intervals[1:]:\n            if ed < s:\n                ans.append([st, ed])\n                st, ed = s, e\n            else:\n                ed = max(ed, e)\n        ans.append([st, ed])\n        return ans\n",
          "reviewedCode": "# [Reader note] Entry point\n# Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nclass Solution:\n    def merge(self, intervals: List[List[int]]) -> List[List[int]]:\n        intervals.sort()\n        # [Reader note] State setup\n        # This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        ans = []\n        st, ed = intervals[0]\n        # [Reader note] Main pass\n        # This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for s, e in intervals[1:]:\n            # [Reader note] Decision point\n            # Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if ed < s:\n                ans.append([st, ed])\n                st, ed = s, e\n            else:\n                ed = max(ed, e)\n        ans.append([st, ed])\n        # [Reader note] Return\n        # The final return exposes the result after the traversal and state updates have settled.\n        return ans",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 4,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 6,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 7,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 13,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        }
      ]
    },
    {
      "id": "cpp",
      "label": "C++",
      "sourceCount": 5,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-cpp-0056-merge-intervals.cpp",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/cpp/0056-merge-intervals.cpp",
          "localPath": "neetcode-gh_leetcode/cpp/0056-merge-intervals.cpp",
          "relativePath": "cpp/0056-merge-intervals.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "article+code",
          "code": "/*\n    Given an array of intervals, merge all overlapping intervals\n    Ex. intervals = [[1,3],[2,6],[8,10],[15,18]] -> [[1,6],[8,10],[15,18]]\n\n    Sort by earliest start time, merge overlapping intervals (take longer end time)\n\n    Time: O(n log n)\n    Space: O(n)\n*/\n\nclass Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        int n = intervals.size();\n        if (n == 1) {\n            return intervals;\n        }\n        \n        sort(intervals.begin(), intervals.end(), [](const auto& a, const auto& b) {\n           return a[0] < b[0]; \n        });\n        \n        vector<vector<int>> result;\n        \n        int i = 0;\n        while (i < n - 1) {\n            if (intervals[i][1] >= intervals[i+1][0]) {\n                intervals[i+1][0] = intervals[i][0];\n                intervals[i+1][1] = max(intervals[i][1], intervals[i+1][1]);\n            } else {\n                result.push_back(intervals[i]);\n            }\n            i++;\n        }\n        result.push_back(intervals[i]);\n        \n        return result;\n    }\n};\n",
          "originalCode": "/*\n    Given an array of intervals, merge all overlapping intervals\n    Ex. intervals = [[1,3],[2,6],[8,10],[15,18]] -> [[1,6],[8,10],[15,18]]\n\n    Sort by earliest start time, merge overlapping intervals (take longer end time)\n\n    Time: O(n log n)\n    Space: O(n)\n*/\n\nclass Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        int n = intervals.size();\n        if (n == 1) {\n            return intervals;\n        }\n        \n        sort(intervals.begin(), intervals.end(), [](const auto& a, const auto& b) {\n           return a[0] < b[0]; \n        });\n        \n        vector<vector<int>> result;\n        \n        int i = 0;\n        while (i < n - 1) {\n            if (intervals[i][1] >= intervals[i+1][0]) {\n                intervals[i+1][0] = intervals[i][0];\n                intervals[i+1][1] = max(intervals[i][1], intervals[i+1][1]);\n            } else {\n                result.push_back(intervals[i]);\n            }\n            i++;\n        }\n        result.push_back(intervals[i]);\n        \n        return result;\n    }\n};\n",
          "reviewedCode": "/*\n    Given an array of intervals, merge all overlapping intervals\n    Ex. intervals = [[1,3],[2,6],[8,10],[15,18]] -> [[1,6],[8,10],[15,18]]\n\n    Sort by earliest start time, merge overlapping intervals (take longer end time)\n\n    Time: O(n log n)\n    Space: O(n)\n*/\n\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nclass Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        int n = intervals.size();\n        if (n == 1) {\n            return intervals;\n        }\n        \n        sort(intervals.begin(), intervals.end(), [](const auto& a, const auto& b) {\n           return a[0] < b[0]; \n        });\n        \n        vector<vector<int>> result;\n        \n        int i = 0;\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        while (i < n - 1) {\n            // [Reader note] Decision point\n            // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if (intervals[i][1] >= intervals[i+1][0]) {\n                intervals[i+1][0] = intervals[i][0];\n                intervals[i+1][1] = max(intervals[i][1], intervals[i+1][1]);\n            } else {\n                result.push_back(intervals[i]);\n            }\n            i++;\n        }\n        result.push_back(intervals[i]);\n        \n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return result;\n    }\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [
            "Sort by earliest start time, merge overlapping intervals (take longer end time)",
            "Time: O(n log n)",
            "Space: O(n)"
          ],
          "annotations": [
            {
              "line": 11,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 14,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 26,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 27,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 37,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        },
        {
          "id": "kamyu104_leetcode-solutions-c-merge-intervals.cpp",
          "repository": "kamyu104_LeetCode-Solutions",
          "repositoryUrl": "https://github.com/kamyu104/LeetCode-Solutions",
          "sourceUrl": "https://github.com/kamyu104/LeetCode-Solutions/blob/master/C%2B%2B/merge-intervals.cpp",
          "localPath": "kamyu104_LeetCode-Solutions/C++/merge-intervals.cpp",
          "relativePath": "C++/merge-intervals.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "indexed-code",
          "code": "// Time:  O(nlogn)\n// Space: O(1)\n\nclass Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        sort(begin(intervals), end(intervals));\n        vector<vector<int>> result;\n        for (const auto& interval : intervals) {\n            if (empty(result) || interval[0] > result.back()[1]) { \n                result.emplace_back(interval);\n            } else {\n                result.back()[1] = max(result.back()[1], interval[1]);\n            }\n        }\n        return result;\n    }\n};\n",
          "originalCode": "// Time:  O(nlogn)\n// Space: O(1)\n\nclass Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        sort(begin(intervals), end(intervals));\n        vector<vector<int>> result;\n        for (const auto& interval : intervals) {\n            if (empty(result) || interval[0] > result.back()[1]) { \n                result.emplace_back(interval);\n            } else {\n                result.back()[1] = max(result.back()[1], interval[1]);\n            }\n        }\n        return result;\n    }\n};\n",
          "reviewedCode": "// Time:  O(nlogn)\n// Space: O(1)\n\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nclass Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        sort(begin(intervals), end(intervals));\n        vector<vector<int>> result;\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        for (const auto& interval : intervals) {\n            // [Reader note] Decision point\n            // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if (empty(result) || interval[0] > result.back()[1]) { \n                result.emplace_back(interval);\n            } else {\n                result.back()[1] = max(result.back()[1], interval[1]);\n            }\n        }\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return result;\n    }\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [
            "Time:  O(nlogn)",
            "Space: O(1)"
          ],
          "annotations": [
            {
              "line": 4,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 9,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 10,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 16,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        },
        {
          "id": "doocs_leetcode-solution-0000-0099-0056.merge-intervals-solution.cpp",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0056.Merge%20Intervals/Solution.cpp",
          "localPath": "doocs_leetcode/solution/0000-0099/0056.Merge Intervals/Solution.cpp",
          "relativePath": "solution/0000-0099/0056.Merge Intervals/Solution.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "code+explanation",
          "code": "class Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        sort(intervals.begin(), intervals.end());\n        int st = intervals[0][0], ed = intervals[0][1];\n        vector<vector<int>> ans;\n        for (int i = 1; i < intervals.size(); ++i) {\n            if (ed < intervals[i][0]) {\n                ans.push_back({st, ed});\n                st = intervals[i][0];\n                ed = intervals[i][1];\n            } else {\n                ed = max(ed, intervals[i][1]);\n            }\n        }\n        ans.push_back({st, ed});\n        return ans;\n    }\n};",
          "originalCode": "class Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        sort(intervals.begin(), intervals.end());\n        int st = intervals[0][0], ed = intervals[0][1];\n        vector<vector<int>> ans;\n        for (int i = 1; i < intervals.size(); ++i) {\n            if (ed < intervals[i][0]) {\n                ans.push_back({st, ed});\n                st = intervals[i][0];\n                ed = intervals[i][1];\n            } else {\n                ed = max(ed, intervals[i][1]);\n            }\n        }\n        ans.push_back({st, ed});\n        return ans;\n    }\n};",
          "reviewedCode": "// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nclass Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        sort(intervals.begin(), intervals.end());\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        int st = intervals[0][0], ed = intervals[0][1];\n        vector<vector<int>> ans;\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (int i = 1; i < intervals.size(); ++i) {\n            // [Reader note] Decision point\n            // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if (ed < intervals[i][0]) {\n                ans.push_back({st, ed});\n                st = intervals[i][0];\n                ed = intervals[i][1];\n            } else {\n                ed = max(ed, intervals[i][1]);\n            }\n        }\n        ans.push_back({st, ed});\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return ans;\n    }\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 5,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 7,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 8,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 17,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        },
        {
          "id": "wisdompeak_leetcode-others-056.merge-intervals-056.merge-intervals.cpp",
          "repository": "wisdompeak_LeetCode",
          "repositoryUrl": "https://github.com/wisdompeak/LeetCode",
          "sourceUrl": "https://github.com/wisdompeak/LeetCode/blob/master/Others/056.Merge-Intervals/056.Merge-Intervals.cpp",
          "localPath": "wisdompeak_LeetCode/Others/056.Merge-Intervals/056.Merge-Intervals.cpp",
          "relativePath": "Others/056.Merge-Intervals/056.Merge-Intervals.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "code+readme",
          "code": "class Solution {\n    static bool cmp(pair<int,int>&a, pair<int,int>&b)\n    {\n        if (a.first!=b.first)\n            return a.first <b.first;\n        else\n            return a.second > b.second;\n    }\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) \n    {\n        vector<pair<int,int>>diff;\n        for (auto& interval: intervals)\n        {\n            diff.push_back({interval[0], 1});\n            diff.push_back({interval[1], -1});\n        }\n\n        sort(diff.begin(), diff.end(), cmp);\n        \n        vector<vector<int>>rets;\n        int start = 0, end = 0;   \n        int sum = 0;\n        for (int i=0; i<diff.size(); i++)\n        {            \n            if (sum==0 && diff[i].second > 0)\n            {\n                start = diff[i].first;                \n            }\n            else if (sum > 0 && sum + diff[i].second == 0)\n            {\n                end = diff[i].first;\n                rets.push_back({start,end});\n            }\n            sum += diff[i].second;            \n        }\n\n        return rets;\n    }\n};\n",
          "originalCode": "class Solution {\n    static bool cmp(pair<int,int>&a, pair<int,int>&b)\n    {\n        if (a.first!=b.first)\n            return a.first <b.first;\n        else\n            return a.second > b.second;\n    }\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) \n    {\n        vector<pair<int,int>>diff;\n        for (auto& interval: intervals)\n        {\n            diff.push_back({interval[0], 1});\n            diff.push_back({interval[1], -1});\n        }\n\n        sort(diff.begin(), diff.end(), cmp);\n        \n        vector<vector<int>>rets;\n        int start = 0, end = 0;   \n        int sum = 0;\n        for (int i=0; i<diff.size(); i++)\n        {            \n            if (sum==0 && diff[i].second > 0)\n            {\n                start = diff[i].first;                \n            }\n            else if (sum > 0 && sum + diff[i].second == 0)\n            {\n                end = diff[i].first;\n                rets.push_back({start,end});\n            }\n            sum += diff[i].second;            \n        }\n\n        return rets;\n    }\n};\n",
          "reviewedCode": "// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nclass Solution {\n    static bool cmp(pair<int,int>&a, pair<int,int>&b)\n    {\n        if (a.first!=b.first)\n            return a.first <b.first;\n        else\n            return a.second > b.second;\n    }\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) \n    {\n        vector<pair<int,int>>diff;\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (auto& interval: intervals)\n        {\n            diff.push_back({interval[0], 1});\n            diff.push_back({interval[1], -1});\n        }\n\n        sort(diff.begin(), diff.end(), cmp);\n        \n        vector<vector<int>>rets;\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        int start = 0, end = 0;   \n        int sum = 0;\n        for (int i=0; i<diff.size(); i++)\n        {            \n            // [Reader note] Decision point\n            // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if (sum==0 && diff[i].second > 0)\n            {\n                start = diff[i].first;                \n            }\n            else if (sum > 0 && sum + diff[i].second == 0)\n            {\n                end = diff[i].first;\n                rets.push_back({start,end});\n            }\n            sum += diff[i].second;            \n        }\n\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return rets;\n    }\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 22,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 13,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 26,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 38,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        },
        {
          "id": "haoel_leetcode-algorithms-cpp-mergeintervals-mergeintervals.cpp",
          "repository": "haoel_leetcode",
          "repositoryUrl": "https://github.com/haoel/leetcode",
          "sourceUrl": "https://github.com/haoel/leetcode/blob/master/algorithms/cpp/mergeIntervals/mergeIntervals.cpp",
          "localPath": "haoel_leetcode/algorithms/cpp/mergeIntervals/mergeIntervals.cpp",
          "relativePath": "algorithms/cpp/mergeIntervals/mergeIntervals.cpp",
          "languageId": "cpp",
          "languageLabel": "C++",
          "coverageType": "indexed-code",
          "code": "// Source : https://oj.leetcode.com/problems/merge-intervals/\n// Author : Hao Chen\n// Date   : 2014-08-26\n\n/********************************************************************************** \n* \n* Given a collection of intervals, merge all overlapping intervals.\n* \n* For example,\n* Given [1,3],[2,6],[8,10],[15,18],\n* return [1,6],[8,10],[15,18].\n* \n*               \n**********************************************************************************/\n\n#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nstruct Interval {\n    int start;\n    int end;\n    Interval() : start(0), end(0) {}\n    Interval(int s, int e) : start(s), end(e) {}\n};\n\n//Two factos sorting [start:end]\nbool compare(const Interval& lhs, const Interval& rhs){\n    return (lhs.start==rhs.start) ? lhs.end < rhs.end : lhs.start < rhs.start;\n}\n\nvector<Interval> merge(vector<Interval> &intervals) {\n\n    vector<Interval> result;\n\n    if (intervals.size() <= 0) return result;\n    //sort the inervals. Note: using the customized comparing function.\n    sort(intervals.begin(), intervals.end(), compare);\n    for(int i=0; i<intervals.size(); i++) { \n        int size = result.size();\n        // if the current intervals[i] is overlapped with previous interval.\n        // merge them together\n        if( size>0 && result[size-1].end >= intervals[i].start) {\n            result[size-1].end = max(result[size-1].end, intervals[i].end); \n        }else{\n            result.push_back(intervals[i]);\n        }\n    }\n    \n    return result;\n}\n\n\nint main(int argc, char**argv)\n{\n    Interval i1(1,4);\n    Interval i2(0,2);\n    Interval i3(3,5);\n    Interval i4(15,18);\n    vector<Interval> intervals;\n    intervals.push_back(i1);\n    intervals.push_back(i2);\n    intervals.push_back(i3);\n    intervals.push_back(i4);\n    \n    vector<Interval> r = merge(intervals);\n    \n    for(int i=0; i<r.size(); i++){\n        cout << \"[ \" << r[i].start << \", \" << r[i].end << \" ] \";\n    }\n    cout <<endl;\n\n    return 0;\n}\n",
          "originalCode": "// Source : https://oj.leetcode.com/problems/merge-intervals/\n// Author : Hao Chen\n// Date   : 2014-08-26\n\n/********************************************************************************** \n* \n* Given a collection of intervals, merge all overlapping intervals.\n* \n* For example,\n* Given [1,3],[2,6],[8,10],[15,18],\n* return [1,6],[8,10],[15,18].\n* \n*               \n**********************************************************************************/\n\n#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nstruct Interval {\n    int start;\n    int end;\n    Interval() : start(0), end(0) {}\n    Interval(int s, int e) : start(s), end(e) {}\n};\n\n//Two factos sorting [start:end]\nbool compare(const Interval& lhs, const Interval& rhs){\n    return (lhs.start==rhs.start) ? lhs.end < rhs.end : lhs.start < rhs.start;\n}\n\nvector<Interval> merge(vector<Interval> &intervals) {\n\n    vector<Interval> result;\n\n    if (intervals.size() <= 0) return result;\n    //sort the inervals. Note: using the customized comparing function.\n    sort(intervals.begin(), intervals.end(), compare);\n    for(int i=0; i<intervals.size(); i++) { \n        int size = result.size();\n        // if the current intervals[i] is overlapped with previous interval.\n        // merge them together\n        if( size>0 && result[size-1].end >= intervals[i].start) {\n            result[size-1].end = max(result[size-1].end, intervals[i].end); \n        }else{\n            result.push_back(intervals[i]);\n        }\n    }\n    \n    return result;\n}\n\n\nint main(int argc, char**argv)\n{\n    Interval i1(1,4);\n    Interval i2(0,2);\n    Interval i3(3,5);\n    Interval i4(15,18);\n    vector<Interval> intervals;\n    intervals.push_back(i1);\n    intervals.push_back(i2);\n    intervals.push_back(i3);\n    intervals.push_back(i4);\n    \n    vector<Interval> r = merge(intervals);\n    \n    for(int i=0; i<r.size(); i++){\n        cout << \"[ \" << r[i].start << \", \" << r[i].end << \" ] \";\n    }\n    cout <<endl;\n\n    return 0;\n}\n",
          "reviewedCode": "// Source : https://oj.leetcode.com/problems/merge-intervals/\n// Author : Hao Chen\n// Date   : 2014-08-26\n\n/********************************************************************************** \n* \n* Given a collection of intervals, merge all overlapping intervals.\n* \n* For example,\n* Given [1,3],[2,6],[8,10],[15,18],\n* return [1,6],[8,10],[15,18].\n* \n*               \n**********************************************************************************/\n\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\n#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nstruct Interval {\n    int start;\n    int end;\n    Interval() : start(0), end(0) {}\n    Interval(int s, int e) : start(s), end(e) {}\n};\n\n//Two factos sorting [start:end]\n// [Reader note] State setup\n// This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\nbool compare(const Interval& lhs, const Interval& rhs){\n    return (lhs.start==rhs.start) ? lhs.end < rhs.end : lhs.start < rhs.start;\n}\n\nvector<Interval> merge(vector<Interval> &intervals) {\n\n    vector<Interval> result;\n\n    if (intervals.size() <= 0) return result;\n    //sort the inervals. Note: using the customized comparing function.\n    sort(intervals.begin(), intervals.end(), compare);\n    // [Reader note] Main pass\n    // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n    for(int i=0; i<intervals.size(); i++) { \n        int size = result.size();\n        // [Reader note] Decision point\n        // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n        // if the current intervals[i] is overlapped with previous interval.\n        // merge them together\n        if( size>0 && result[size-1].end >= intervals[i].start) {\n            result[size-1].end = max(result[size-1].end, intervals[i].end); \n        }else{\n            result.push_back(intervals[i]);\n        }\n    }\n    \n    return result;\n}\n\n\nint main(int argc, char**argv)\n{\n    Interval i1(1,4);\n    Interval i2(0,2);\n    Interval i3(3,5);\n    Interval i4(15,18);\n    vector<Interval> intervals;\n    intervals.push_back(i1);\n    intervals.push_back(i2);\n    intervals.push_back(i3);\n    intervals.push_back(i4);\n    \n    vector<Interval> r = merge(intervals);\n    \n    for(int i=0; i<r.size(); i++){\n        cout << \"[ \" << r[i].start << \", \" << r[i].end << \" ] \";\n    }\n    cout <<endl;\n\n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return 0;\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 16,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 29,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 40,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 42,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 74,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        }
      ]
    },
    {
      "id": "java",
      "label": "Java",
      "sourceCount": 2,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-java-0056-merge-intervals.java",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/java/0056-merge-intervals.java",
          "localPath": "neetcode-gh_leetcode/java/0056-merge-intervals.java",
          "relativePath": "java/0056-merge-intervals.java",
          "languageId": "java",
          "languageLabel": "Java",
          "coverageType": "article+code",
          "code": "class Solution {\n\n    public int[][] merge(int[][] intervals) {\n        ArrayList<int[]> ans = new ArrayList<>();\n        Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));\n        ans.add(intervals[0]);\n        for (int i = 1; i < intervals.length; i++) {\n            //comparing the values of prevEnd and curStart\n            int curStart = intervals[i][0];\n            if (curStart <= ans.get(ans.size() - 1)[1]) {\n                //do the merging\n                ans.get(ans.size() - 1)[1] =\n                    Math.max(ans.get(ans.size() - 1)[1], intervals[i][1]);\n            } else {\n                ans.add(intervals[i]);\n            }\n        }\n        int[][] res = new int[ans.size()][2];\n        ans.toArray(res);\n        return res;\n    }\n}\n",
          "originalCode": "class Solution {\n\n    public int[][] merge(int[][] intervals) {\n        ArrayList<int[]> ans = new ArrayList<>();\n        Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));\n        ans.add(intervals[0]);\n        for (int i = 1; i < intervals.length; i++) {\n            //comparing the values of prevEnd and curStart\n            int curStart = intervals[i][0];\n            if (curStart <= ans.get(ans.size() - 1)[1]) {\n                //do the merging\n                ans.get(ans.size() - 1)[1] =\n                    Math.max(ans.get(ans.size() - 1)[1], intervals[i][1]);\n            } else {\n                ans.add(intervals[i]);\n            }\n        }\n        int[][] res = new int[ans.size()][2];\n        ans.toArray(res);\n        return res;\n    }\n}\n",
          "reviewedCode": "// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nclass Solution {\n\n    public int[][] merge(int[][] intervals) {\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        ArrayList<int[]> ans = new ArrayList<>();\n        Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));\n        ans.add(intervals[0]);\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (int i = 1; i < intervals.length; i++) {\n            //comparing the values of prevEnd and curStart\n            int curStart = intervals[i][0];\n            // [Reader note] Decision point\n            // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if (curStart <= ans.get(ans.size() - 1)[1]) {\n                //do the merging\n                ans.get(ans.size() - 1)[1] =\n                    Math.max(ans.get(ans.size() - 1)[1], intervals[i][1]);\n            } else {\n                ans.add(intervals[i]);\n            }\n        }\n        int[][] res = new int[ans.size()][2];\n        ans.toArray(res);\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return res;\n    }\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 4,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 7,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 10,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 20,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        },
        {
          "id": "doocs_leetcode-solution-0000-0099-0056.merge-intervals-solution.java",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0056.Merge%20Intervals/Solution.java",
          "localPath": "doocs_leetcode/solution/0000-0099/0056.Merge Intervals/Solution.java",
          "relativePath": "solution/0000-0099/0056.Merge Intervals/Solution.java",
          "languageId": "java",
          "languageLabel": "Java",
          "coverageType": "code+explanation",
          "code": "class Solution {\n    public int[][] merge(int[][] intervals) {\n        Arrays.sort(intervals, Comparator.comparingInt(a -> a[0]));\n        int st = intervals[0][0], ed = intervals[0][1];\n        List<int[]> ans = new ArrayList<>();\n        for (int i = 1; i < intervals.length; ++i) {\n            int s = intervals[i][0], e = intervals[i][1];\n            if (ed < s) {\n                ans.add(new int[] {st, ed});\n                st = s;\n                ed = e;\n            } else {\n                ed = Math.max(ed, e);\n            }\n        }\n        ans.add(new int[] {st, ed});\n        return ans.toArray(new int[ans.size()][]);\n    }\n}",
          "originalCode": "class Solution {\n    public int[][] merge(int[][] intervals) {\n        Arrays.sort(intervals, Comparator.comparingInt(a -> a[0]));\n        int st = intervals[0][0], ed = intervals[0][1];\n        List<int[]> ans = new ArrayList<>();\n        for (int i = 1; i < intervals.length; ++i) {\n            int s = intervals[i][0], e = intervals[i][1];\n            if (ed < s) {\n                ans.add(new int[] {st, ed});\n                st = s;\n                ed = e;\n            } else {\n                ed = Math.max(ed, e);\n            }\n        }\n        ans.add(new int[] {st, ed});\n        return ans.toArray(new int[ans.size()][]);\n    }\n}",
          "reviewedCode": "// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nclass Solution {\n    public int[][] merge(int[][] intervals) {\n        Arrays.sort(intervals, Comparator.comparingInt(a -> a[0]));\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        int st = intervals[0][0], ed = intervals[0][1];\n        List<int[]> ans = new ArrayList<>();\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (int i = 1; i < intervals.length; ++i) {\n            int s = intervals[i][0], e = intervals[i][1];\n            // [Reader note] Decision point\n            // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if (ed < s) {\n                ans.add(new int[] {st, ed});\n                st = s;\n                ed = e;\n            } else {\n                ed = Math.max(ed, e);\n            }\n        }\n        ans.add(new int[] {st, ed});\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return ans.toArray(new int[ans.size()][]);\n    }\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 4,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 6,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 8,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 17,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        }
      ]
    },
    {
      "id": "go",
      "label": "Go",
      "sourceCount": 2,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-go-0056-merge-intervals.go",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/go/0056-merge-intervals.go",
          "localPath": "neetcode-gh_leetcode/go/0056-merge-intervals.go",
          "relativePath": "go/0056-merge-intervals.go",
          "languageId": "go",
          "languageLabel": "Go",
          "coverageType": "article+code",
          "code": "import \"sort\"\n\nfunc merge(intervals [][]int) [][]int {\n\tconst (\n\t\tstart = 0\n\t\tend   = 1\n\t)\n\n\tsort.SliceStable(intervals, func(i, j int) bool {\n\t\treturn intervals[i][start] < intervals[j][start]\n\t})\n\n\tres := [][]int{intervals[0]}\n\tfor i := 1; i < len(intervals); i++ {\n\t\tcurrResLastIndex := len(res) - 1\n\t\tcurrEnd := res[currResLastIndex]\n\t\tcurr := intervals[i]\n\n\t\tif currEnd[end] < curr[start] {\n\t\t\tres = append(res, curr)\n\t\t} else if currEnd[end] < curr[end] {\n\t\t\tres[currResLastIndex][end] = curr[end]\n\t\t}\n\t}\n\n\treturn res\n}",
          "originalCode": "import \"sort\"\n\nfunc merge(intervals [][]int) [][]int {\n\tconst (\n\t\tstart = 0\n\t\tend   = 1\n\t)\n\n\tsort.SliceStable(intervals, func(i, j int) bool {\n\t\treturn intervals[i][start] < intervals[j][start]\n\t})\n\n\tres := [][]int{intervals[0]}\n\tfor i := 1; i < len(intervals); i++ {\n\t\tcurrResLastIndex := len(res) - 1\n\t\tcurrEnd := res[currResLastIndex]\n\t\tcurr := intervals[i]\n\n\t\tif currEnd[end] < curr[start] {\n\t\t\tres = append(res, curr)\n\t\t} else if currEnd[end] < curr[end] {\n\t\t\tres[currResLastIndex][end] = curr[end]\n\t\t}\n\t}\n\n\treturn res\n}",
          "reviewedCode": "import \"sort\"\n\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nfunc merge(intervals [][]int) [][]int {\n\t// [Reader note] State setup\n\t// This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n\tconst (\n\t\tstart = 0\n\t\tend   = 1\n\t)\n\n\tsort.SliceStable(intervals, func(i, j int) bool {\n\t\treturn intervals[i][start] < intervals[j][start]\n\t})\n\n\tres := [][]int{intervals[0]}\n\t// [Reader note] Main pass\n\t// This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n\tfor i := 1; i < len(intervals); i++ {\n\t\tcurrResLastIndex := len(res) - 1\n\t\tcurrEnd := res[currResLastIndex]\n\t\tcurr := intervals[i]\n\n\t\t// [Reader note] Decision point\n\t\t// Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n\t\tif currEnd[end] < curr[start] {\n\t\t\tres = append(res, curr)\n\t\t} else if currEnd[end] < curr[end] {\n\t\t\tres[currResLastIndex][end] = curr[end]\n\t\t}\n\t}\n\n\t// [Reader note] Return\n\t// The final return exposes the result after the traversal and state updates have settled.\n\treturn res\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 3,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 4,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 14,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 19,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 26,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        },
        {
          "id": "doocs_leetcode-solution-0000-0099-0056.merge-intervals-solution.go",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0056.Merge%20Intervals/Solution.go",
          "localPath": "doocs_leetcode/solution/0000-0099/0056.Merge Intervals/Solution.go",
          "relativePath": "solution/0000-0099/0056.Merge Intervals/Solution.go",
          "languageId": "go",
          "languageLabel": "Go",
          "coverageType": "code+explanation",
          "code": "func merge(intervals [][]int) (ans [][]int) {\n\tsort.Slice(intervals, func(i, j int) bool {\n\t\treturn intervals[i][0] < intervals[j][0]\n\t})\n\tst, ed := intervals[0][0], intervals[0][1]\n\tfor _, e := range intervals[1:] {\n\t\tif ed < e[0] {\n\t\t\tans = append(ans, []int{st, ed})\n\t\t\tst, ed = e[0], e[1]\n\t\t} else if ed < e[1] {\n\t\t\ted = e[1]\n\t\t}\n\t}\n\tans = append(ans, []int{st, ed})\n\treturn ans\n}",
          "originalCode": "func merge(intervals [][]int) (ans [][]int) {\n\tsort.Slice(intervals, func(i, j int) bool {\n\t\treturn intervals[i][0] < intervals[j][0]\n\t})\n\tst, ed := intervals[0][0], intervals[0][1]\n\tfor _, e := range intervals[1:] {\n\t\tif ed < e[0] {\n\t\t\tans = append(ans, []int{st, ed})\n\t\t\tst, ed = e[0], e[1]\n\t\t} else if ed < e[1] {\n\t\t\ted = e[1]\n\t\t}\n\t}\n\tans = append(ans, []int{st, ed})\n\treturn ans\n}",
          "reviewedCode": "// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nfunc merge(intervals [][]int) (ans [][]int) {\n\tsort.Slice(intervals, func(i, j int) bool {\n\t\treturn intervals[i][0] < intervals[j][0]\n\t})\n\t// [Reader note] State setup\n\t// This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n\tst, ed := intervals[0][0], intervals[0][1]\n\t// [Reader note] Main pass\n\t// This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n\tfor _, e := range intervals[1:] {\n\t\t// [Reader note] Decision point\n\t\t// Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n\t\tif ed < e[0] {\n\t\t\tans = append(ans, []int{st, ed})\n\t\t\tst, ed = e[0], e[1]\n\t\t} else if ed < e[1] {\n\t\t\ted = e[1]\n\t\t}\n\t}\n\tans = append(ans, []int{st, ed})\n\t// [Reader note] Return\n\t// The final return exposes the result after the traversal and state updates have settled.\n\treturn ans\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 5,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 6,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 7,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 15,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        }
      ]
    },
    {
      "id": "typescript",
      "label": "TypeScript",
      "sourceCount": 1,
      "sources": [
        {
          "id": "doocs_leetcode-solution-0000-0099-0056.merge-intervals-solution.ts",
          "repository": "doocs_leetcode",
          "repositoryUrl": "https://github.com/doocs/leetcode",
          "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0056.Merge%20Intervals/Solution.ts",
          "localPath": "doocs_leetcode/solution/0000-0099/0056.Merge Intervals/Solution.ts",
          "relativePath": "solution/0000-0099/0056.Merge Intervals/Solution.ts",
          "languageId": "typescript",
          "languageLabel": "TypeScript",
          "coverageType": "code+explanation",
          "code": "function merge(intervals: number[][]): number[][] {\n    intervals.sort((a, b) => a[0] - b[0]);\n    const ans: number[][] = [];\n    let [st, ed] = intervals[0];\n    for (const [s, e] of intervals.slice(1)) {\n        if (ed < s) {\n            ans.push([st, ed]);\n            [st, ed] = [s, e];\n        } else {\n            ed = Math.max(ed, e);\n        }\n    }\n    ans.push([st, ed]);\n    return ans;\n}\n",
          "originalCode": "function merge(intervals: number[][]): number[][] {\n    intervals.sort((a, b) => a[0] - b[0]);\n    const ans: number[][] = [];\n    let [st, ed] = intervals[0];\n    for (const [s, e] of intervals.slice(1)) {\n        if (ed < s) {\n            ans.push([st, ed]);\n            [st, ed] = [s, e];\n        } else {\n            ed = Math.max(ed, e);\n        }\n    }\n    ans.push([st, ed]);\n    return ans;\n}\n",
          "reviewedCode": "// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nfunction merge(intervals: number[][]): number[][] {\n    intervals.sort((a, b) => a[0] - b[0]);\n    // [Reader note] State setup\n    // This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n    const ans: number[][] = [];\n    let [st, ed] = intervals[0];\n    // [Reader note] Main pass\n    // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n    for (const [s, e] of intervals.slice(1)) {\n        // [Reader note] Decision point\n        // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n        if (ed < s) {\n            ans.push([st, ed]);\n            [st, ed] = [s, e];\n        } else {\n            ed = Math.max(ed, e);\n        }\n    }\n    ans.push([st, ed]);\n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return ans;\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 3,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 5,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 6,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 14,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        }
      ]
    },
    {
      "id": "javascript",
      "label": "JavaScript",
      "sourceCount": 1,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-javascript-0056-merge-intervals.js",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/javascript/0056-merge-intervals.js",
          "localPath": "neetcode-gh_leetcode/javascript/0056-merge-intervals.js",
          "relativePath": "javascript/0056-merge-intervals.js",
          "languageId": "javascript",
          "languageLabel": "JavaScript",
          "coverageType": "article+code",
          "code": "/**\n * https://leetcode.com/problems/merge-intervals/\n * Time O(N * logN) | Space O(N)\n * @param {number[][]} intervals\n * @return {number[][]}\n */\nvar merge = function (intervals) {\n    intervals.sort(([aStart, aEnd], [bStart, bEnd]) =>\n        aStart !== bStart ? aStart - bStart : aEnd - bEnd,\n    );\n\n    return mergerInterval(intervals);\n};\n\nconst mergerInterval = (intervals, merged = []) => {\n    let prev = intervals.shift();\n\n    for (const curr of intervals) {\n        const [prevStart, prevEnd] = prev;\n        const [currStart, currEnd] = curr;\n\n        const hasOverlap = currStart <= prevEnd;\n        if (hasOverlap) {\n            prev[1] = Math.max(prev[1], curr[1]);\n            continue;\n        }\n\n        merged.push(prev);\n        prev = curr;\n    }\n\n    return [...merged, prev];\n};\n",
          "originalCode": "/**\n * https://leetcode.com/problems/merge-intervals/\n * Time O(N * logN) | Space O(N)\n * @param {number[][]} intervals\n * @return {number[][]}\n */\nvar merge = function (intervals) {\n    intervals.sort(([aStart, aEnd], [bStart, bEnd]) =>\n        aStart !== bStart ? aStart - bStart : aEnd - bEnd,\n    );\n\n    return mergerInterval(intervals);\n};\n\nconst mergerInterval = (intervals, merged = []) => {\n    let prev = intervals.shift();\n\n    for (const curr of intervals) {\n        const [prevStart, prevEnd] = prev;\n        const [currStart, currEnd] = curr;\n\n        const hasOverlap = currStart <= prevEnd;\n        if (hasOverlap) {\n            prev[1] = Math.max(prev[1], curr[1]);\n            continue;\n        }\n\n        merged.push(prev);\n        prev = curr;\n    }\n\n    return [...merged, prev];\n};\n",
          "reviewedCode": "/**\n * https://leetcode.com/problems/merge-intervals/\n * Time O(N * logN) | Space O(N)\n * @param {number[][]} intervals\n * @return {number[][]}\n */\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nvar merge = function (intervals) {\n    intervals.sort(([aStart, aEnd], [bStart, bEnd]) =>\n        aStart !== bStart ? aStart - bStart : aEnd - bEnd,\n    );\n\n    return mergerInterval(intervals);\n};\n\n// [Reader note] State setup\n// This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\nconst mergerInterval = (intervals, merged = []) => {\n    let prev = intervals.shift();\n\n    // [Reader note] Main pass\n    // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n    for (const curr of intervals) {\n        const [prevStart, prevEnd] = prev;\n        const [currStart, currEnd] = curr;\n\n        const hasOverlap = currStart <= prevEnd;\n        // [Reader note] Decision point\n        // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n        if (hasOverlap) {\n            prev[1] = Math.max(prev[1], curr[1]);\n            continue;\n        }\n\n        merged.push(prev);\n        prev = curr;\n    }\n\n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return [...merged, prev];\n};",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [
            "Time O(N * logN) | Space O(N)"
          ],
          "annotations": [
            {
              "line": 7,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 15,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 18,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 23,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 32,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        }
      ]
    },
    {
      "id": "csharp",
      "label": "C#",
      "sourceCount": 1,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-csharp-0056-merge-intervals.cs",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/csharp/0056-merge-intervals.cs",
          "localPath": "neetcode-gh_leetcode/csharp/0056-merge-intervals.cs",
          "relativePath": "csharp/0056-merge-intervals.cs",
          "languageId": "csharp",
          "languageLabel": "C#",
          "coverageType": "article+code",
          "code": "public class Solution\n{\n    public int[][] Merge(int[][] intervals)\n    {\n\n        var sortedInterval = intervals.Clone() as int[][];\n        Array.Sort(sortedInterval, (a, b) => a[0] - b[0]);\n\n        var mergedInterval = new List<int[]>();\n        var lastInterval = sortedInterval[0];\n        mergedInterval.Add(lastInterval);\n\n        for (var i = 1; i < sortedInterval.Length; i++)\n        {\n            var current = sortedInterval[i];\n            var lastIntervalEnd = lastInterval[1];\n            var nextIntervalEnd = current[1];\n            var nextIntervalStart = current[0];\n\n            if (lastIntervalEnd >= nextIntervalStart)\n                lastInterval[1] = Math.Max(nextIntervalEnd, lastIntervalEnd);\n            else\n            {\n                lastInterval = current;\n                mergedInterval.Add(lastInterval);\n                //lastInterval = current;\n            }\n        }\n\n        return mergedInterval.ToArray();\n    }\n}",
          "originalCode": "public class Solution\n{\n    public int[][] Merge(int[][] intervals)\n    {\n\n        var sortedInterval = intervals.Clone() as int[][];\n        Array.Sort(sortedInterval, (a, b) => a[0] - b[0]);\n\n        var mergedInterval = new List<int[]>();\n        var lastInterval = sortedInterval[0];\n        mergedInterval.Add(lastInterval);\n\n        for (var i = 1; i < sortedInterval.Length; i++)\n        {\n            var current = sortedInterval[i];\n            var lastIntervalEnd = lastInterval[1];\n            var nextIntervalEnd = current[1];\n            var nextIntervalStart = current[0];\n\n            if (lastIntervalEnd >= nextIntervalStart)\n                lastInterval[1] = Math.Max(nextIntervalEnd, lastIntervalEnd);\n            else\n            {\n                lastInterval = current;\n                mergedInterval.Add(lastInterval);\n                //lastInterval = current;\n            }\n        }\n\n        return mergedInterval.ToArray();\n    }\n}",
          "reviewedCode": "// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\npublic class Solution\n{\n    public int[][] Merge(int[][] intervals)\n    {\n\n        // [Reader note] State setup\n        // This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\n        var sortedInterval = intervals.Clone() as int[][];\n        Array.Sort(sortedInterval, (a, b) => a[0] - b[0]);\n\n        var mergedInterval = new List<int[]>();\n        var lastInterval = sortedInterval[0];\n        mergedInterval.Add(lastInterval);\n\n        // [Reader note] Main pass\n        // This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n        for (var i = 1; i < sortedInterval.Length; i++)\n        {\n            var current = sortedInterval[i];\n            var lastIntervalEnd = lastInterval[1];\n            var nextIntervalEnd = current[1];\n            var nextIntervalStart = current[0];\n\n            // [Reader note] Decision point\n            // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n            if (lastIntervalEnd >= nextIntervalStart)\n                lastInterval[1] = Math.Max(nextIntervalEnd, lastIntervalEnd);\n            else\n            {\n                lastInterval = current;\n                mergedInterval.Add(lastInterval);\n                //lastInterval = current;\n            }\n        }\n\n        // [Reader note] Return\n        // The final return exposes the result after the traversal and state updates have settled.\n        return mergedInterval.ToArray();\n    }\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 1,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 6,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 13,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 20,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 30,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        }
      ]
    },
    {
      "id": "c",
      "label": "C",
      "sourceCount": 1,
      "sources": [
        {
          "id": "neetcode-gh_leetcode-c-0056-merge-intervals.c",
          "repository": "neetcode-gh_leetcode",
          "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
          "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/c/0056-merge-intervals.c",
          "localPath": "neetcode-gh_leetcode/c/0056-merge-intervals.c",
          "relativePath": "c/0056-merge-intervals.c",
          "languageId": "c",
          "languageLabel": "C",
          "coverageType": "article+code",
          "code": "// Interval structure\nstruct Interval {\n    int start;\n    int end;\n};\n\n// Function to compare intervals for sorting\nint compareIntervals(const void* a, const void* b) {\n    return ((struct Interval*)a)->start - ((struct Interval*)b)->start;\n}\n\n// Function to merge intervals\nint** merge(int** intervals, int intervalsSize, int* intervalsColSize, int* returnSize, int** returnColumnSizes) {\n    if (intervalsSize <= 1) {\n        *returnSize = intervalsSize;\n        *returnColumnSizes = intervalsColSize;\n        return intervals;\n    }\n\n    // Create an array of Interval structures\n    struct Interval* sortedIntervals = (struct Interval*)malloc(sizeof(struct Interval) * intervalsSize);\n    for (int i = 0; i < intervalsSize; i++) {\n        sortedIntervals[i].start = intervals[i][0];\n        sortedIntervals[i].end = intervals[i][1];\n    }\n\n    // Sort intervals based on start times\n    qsort(sortedIntervals, intervalsSize, sizeof(struct Interval), compareIntervals);\n\n    // Merge intervals\n    struct Interval* mergedIntervals = (struct Interval*)malloc(sizeof(struct Interval) * intervalsSize);\n    int mergedCount = 0;\n\n    for (int i = 0; i < intervalsSize; i++) {\n        if (mergedCount == 0 || mergedIntervals[mergedCount - 1].end < sortedIntervals[i].start) {\n            mergedIntervals[mergedCount++] = sortedIntervals[i];\n        } else {\n            mergedIntervals[mergedCount - 1].end = mergedIntervals[mergedCount - 1].end > sortedIntervals[i].end\n                                                      ? mergedIntervals[mergedCount - 1].end\n                                                      : sortedIntervals[i].end;\n        }\n    }\n\n    // Allocate memory for return arrays\n    int** result = (int**)malloc(sizeof(int*) * mergedCount);\n    *returnColumnSizes = (int*)malloc(sizeof(int) * mergedCount);\n\n    for (int i = 0; i < mergedCount; i++) {\n        result[i] = (int*)malloc(sizeof(int) * 2);\n        result[i][0] = mergedIntervals[i].start;\n        result[i][1] = mergedIntervals[i].end;\n        (*returnColumnSizes)[i] = 2;\n    }\n\n    *returnSize = mergedCount;\n\n    free(sortedIntervals);\n    free(mergedIntervals);\n\n    return result;\n}\n",
          "originalCode": "// Interval structure\nstruct Interval {\n    int start;\n    int end;\n};\n\n// Function to compare intervals for sorting\nint compareIntervals(const void* a, const void* b) {\n    return ((struct Interval*)a)->start - ((struct Interval*)b)->start;\n}\n\n// Function to merge intervals\nint** merge(int** intervals, int intervalsSize, int* intervalsColSize, int* returnSize, int** returnColumnSizes) {\n    if (intervalsSize <= 1) {\n        *returnSize = intervalsSize;\n        *returnColumnSizes = intervalsColSize;\n        return intervals;\n    }\n\n    // Create an array of Interval structures\n    struct Interval* sortedIntervals = (struct Interval*)malloc(sizeof(struct Interval) * intervalsSize);\n    for (int i = 0; i < intervalsSize; i++) {\n        sortedIntervals[i].start = intervals[i][0];\n        sortedIntervals[i].end = intervals[i][1];\n    }\n\n    // Sort intervals based on start times\n    qsort(sortedIntervals, intervalsSize, sizeof(struct Interval), compareIntervals);\n\n    // Merge intervals\n    struct Interval* mergedIntervals = (struct Interval*)malloc(sizeof(struct Interval) * intervalsSize);\n    int mergedCount = 0;\n\n    for (int i = 0; i < intervalsSize; i++) {\n        if (mergedCount == 0 || mergedIntervals[mergedCount - 1].end < sortedIntervals[i].start) {\n            mergedIntervals[mergedCount++] = sortedIntervals[i];\n        } else {\n            mergedIntervals[mergedCount - 1].end = mergedIntervals[mergedCount - 1].end > sortedIntervals[i].end\n                                                      ? mergedIntervals[mergedCount - 1].end\n                                                      : sortedIntervals[i].end;\n        }\n    }\n\n    // Allocate memory for return arrays\n    int** result = (int**)malloc(sizeof(int*) * mergedCount);\n    *returnColumnSizes = (int*)malloc(sizeof(int) * mergedCount);\n\n    for (int i = 0; i < mergedCount; i++) {\n        result[i] = (int*)malloc(sizeof(int) * 2);\n        result[i][0] = mergedIntervals[i].start;\n        result[i][1] = mergedIntervals[i].end;\n        (*returnColumnSizes)[i] = 2;\n    }\n\n    *returnSize = mergedCount;\n\n    free(sortedIntervals);\n    free(mergedIntervals);\n\n    return result;\n}\n",
          "reviewedCode": "// Interval structure\n// [Reader note] Entry point\n// Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it.\nstruct Interval {\n    int start;\n    int end;\n};\n\n// [Reader note] Main pass\n// This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch.\n// Function to compare intervals for sorting\n// [Reader note] State setup\n// This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.\nint compareIntervals(const void* a, const void* b) {\n    return ((struct Interval*)a)->start - ((struct Interval*)b)->start;\n}\n\n// Function to merge intervals\nint** merge(int** intervals, int intervalsSize, int* intervalsColSize, int* returnSize, int** returnColumnSizes) {\n    // [Reader note] Decision point\n    // Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention.\n    if (intervalsSize <= 1) {\n        *returnSize = intervalsSize;\n        *returnColumnSizes = intervalsColSize;\n        return intervals;\n    }\n\n    // Create an array of Interval structures\n    struct Interval* sortedIntervals = (struct Interval*)malloc(sizeof(struct Interval) * intervalsSize);\n    for (int i = 0; i < intervalsSize; i++) {\n        sortedIntervals[i].start = intervals[i][0];\n        sortedIntervals[i].end = intervals[i][1];\n    }\n\n    // Sort intervals based on start times\n    qsort(sortedIntervals, intervalsSize, sizeof(struct Interval), compareIntervals);\n\n    // Merge intervals\n    struct Interval* mergedIntervals = (struct Interval*)malloc(sizeof(struct Interval) * intervalsSize);\n    int mergedCount = 0;\n\n    for (int i = 0; i < intervalsSize; i++) {\n        if (mergedCount == 0 || mergedIntervals[mergedCount - 1].end < sortedIntervals[i].start) {\n            mergedIntervals[mergedCount++] = sortedIntervals[i];\n        } else {\n            mergedIntervals[mergedCount - 1].end = mergedIntervals[mergedCount - 1].end > sortedIntervals[i].end\n                                                      ? mergedIntervals[mergedCount - 1].end\n                                                      : sortedIntervals[i].end;\n        }\n    }\n\n    // Allocate memory for return arrays\n    int** result = (int**)malloc(sizeof(int*) * mergedCount);\n    *returnColumnSizes = (int*)malloc(sizeof(int) * mergedCount);\n\n    for (int i = 0; i < mergedCount; i++) {\n        result[i] = (int*)malloc(sizeof(int) * 2);\n        result[i][0] = mergedIntervals[i].start;\n        result[i][1] = mergedIntervals[i].end;\n        (*returnColumnSizes)[i] = 2;\n    }\n\n    *returnSize = mergedCount;\n\n    free(sortedIntervals);\n    free(mergedIntervals);\n\n    // [Reader note] Return\n    // The final return exposes the result after the traversal and state updates have settled.\n    return result;\n}",
          "embeddedSuccessfully": true,
          "readerEnhancementsAdded": true,
          "enhancementNote": "Based on the attributed upstream source file. Reader-oriented explanatory comments were added for study use.",
          "complexityHints": [],
          "annotations": [
            {
              "line": 2,
              "label": "Entry point",
              "note": "Start from the core routine and keep this mental model in view: Order the world first, then maintain one current region until the next item truly breaks it."
            },
            {
              "line": 8,
              "label": "State setup",
              "note": "This setup stage creates the state needed for the invariant or partial result: Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work."
            },
            {
              "line": 7,
              "label": "Main pass",
              "note": "This is the main sweep where the implementation earns its efficiency instead of restarting work from scratch."
            },
            {
              "line": 14,
              "label": "Decision point",
              "note": "Pay attention here: The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
            },
            {
              "line": 60,
              "label": "Return",
              "note": "The final return exposes the result after the traversal and state updates have settled."
            }
          ],
          "implementationReview": {
            "whyThisVersion": "It builds the recurring interview instinct that many interval problems become manageable once the data is ordered.",
            "whatToNotice": "Sorting for structure, maintaining a running merged region, and deciding when to extend versus flush work.",
            "riskPoint": "The implementation is not hard, but edge cases around touching and nested intervals deserve attention."
          }
        }
      ]
    }
  ],
  "writeups": [
    {
      "id": "neetcode-gh_leetcode-articles-merge-intervals.md",
      "repository": "neetcode-gh_leetcode",
      "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
      "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/articles/merge-intervals.md",
      "localPath": "neetcode-gh_leetcode/articles/merge-intervals.md",
      "relativePath": "articles/merge-intervals.md",
      "title": "merge intervals",
      "excerpt": "## Prerequisites Before attempting this problem, you should be comfortable with: - **Sorting** - Required to process intervals in order by start time for efficient merging - **Intervals** - Understanding how to represent, compare, and merge overlapping ranges - **Sweep Line Algorithm** - Alternative approach that tracks active intervals using events on a timeline --- ## 1. Sorting ### Intuition We are given a list o..."
    },
    {
      "id": "neetcode-gh_leetcode-hints-merge-intervals.md",
      "repository": "neetcode-gh_leetcode",
      "repositoryUrl": "https://github.com/neetcode-gh/leetcode",
      "sourceUrl": "https://github.com/neetcode-gh/leetcode/blob/main/hints/merge-intervals.md",
      "localPath": "neetcode-gh_leetcode/hints/merge-intervals.md",
      "relativePath": "hints/merge-intervals.md",
      "title": "merge intervals",
      "excerpt": "<br> <details class=\"hint-accordion\"> <summary>Recommended Time & Space Complexity</summary> <p> You should aim for a solution as good or better than <code>O(nlogn)</code> time and <code>O(n)</code> space, where <code>n</code> is the size of the input array. </p> </details> <br> <details class=\"hint-accordion\"> <summary>Hint 1</summary> <p> Sorting the given intervals in ascending order based on their start values i..."
    },
    {
      "id": "doocs_leetcode-solution-0000-0099-0056.merge-intervals-readme.md",
      "repository": "doocs_leetcode",
      "repositoryUrl": "https://github.com/doocs/leetcode",
      "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0056.Merge%20Intervals/README.md",
      "localPath": "doocs_leetcode/solution/0000-0099/0056.Merge Intervals/README.md",
      "relativePath": "solution/0000-0099/0056.Merge Intervals/README.md",
      "title": "[56. \u5408\u5e76\u533a\u95f4](https://leetcode.cn/problems/merge-intervals)",
      "excerpt": "--- comments: true difficulty: \u4e2d\u7b49 edit_url: https://github.com/doocs/leetcode/edit/main/solution/0000-0099/0056.Merge%20Intervals/README.md tags: - \u6570\u7ec4 - \u6392\u5e8f --- <!-- problem:start --> # [56. \u5408\u5e76\u533a\u95f4](https://leetcode.cn/problems/merge-intervals) [English Version](/solution/0000-0099/0056.Merge%20Intervals/README_EN.md) ## \u9898\u76ee\u63cf\u8ff0 <!-- description:start --> <p>\u4ee5\u6570\u7ec4 <code>intervals</code> \u8868\u793a\u82e5\u5e72\u4e2a\u533a\u95f4\u7684\u96c6\u5408\uff0c\u5176\u4e2d\u5355\u4e2a\u533a\u95f4\u4e3a <code>intervals[i]..."
    },
    {
      "id": "doocs_leetcode-solution-0000-0099-0056.merge-intervals-readme_en.md",
      "repository": "doocs_leetcode",
      "repositoryUrl": "https://github.com/doocs/leetcode",
      "sourceUrl": "https://github.com/doocs/leetcode/blob/main/solution/0000-0099/0056.Merge%20Intervals/README_EN.md",
      "localPath": "doocs_leetcode/solution/0000-0099/0056.Merge Intervals/README_EN.md",
      "relativePath": "solution/0000-0099/0056.Merge Intervals/README_EN.md",
      "title": "[56. Merge Intervals](https://leetcode.com/problems/merge-intervals)",
      "excerpt": "--- comments: true difficulty: Medium edit_url: https://github.com/doocs/leetcode/edit/main/solution/0000-0099/0056.Merge%20Intervals/README_EN.md tags: - Array - Sorting --- <!-- problem:start --> # [56. Merge Intervals](https://leetcode.com/problems/merge-intervals) [\u4e2d\u6587\u6587\u6863](/solution/0000-0099/0056.Merge%20Intervals/README.md) ## Description <!-- description:start --> <p>Given an array&nbsp;of <code>intervals</code..."
    },
    {
      "id": "wisdompeak_leetcode-others-056.merge-intervals-readme.md",
      "repository": "wisdompeak_LeetCode",
      "repositoryUrl": "https://github.com/wisdompeak/LeetCode",
      "sourceUrl": "https://github.com/wisdompeak/LeetCode/blob/master/Others/056.Merge-Intervals/Readme.md",
      "localPath": "wisdompeak_LeetCode/Others/056.Merge-Intervals/Readme.md",
      "relativePath": "Others/056.Merge-Intervals/Readme.md",
      "title": "056.Merge-Intervals",
      "excerpt": "### 056.Merge-Intervals \u5bf9\u4e8e\u533a\u95f4\u5408\u5e76\u7684\u9898\u76ee\uff0c\u4e00\u822c\u90fd\u4f1a\u91c7\u7528\u548c252\u7c7b\u4f3c\u7684\u201c\u626b\u63cf\u7ebf\u201d\u7b97\u6cd5\u3002\u5bf9\u4e8e\u6bcf\u4e00\u4e2a\u533a\u95f4[a,b]\uff0c\u6211\u4eec\u5728a\u65f6\u523b\u8bb0\u5f55+1\uff0c\u5728b\u65f6\u523b\u8bb0\u5f55-1. \u7136\u540e\u6211\u4eec\u518d\u5728\u65f6\u95f4\u8f74\u4e0a\u987a\u6b21\u904d\u5386\u6bcf\u4e00\u4e2a\u65f6\u95f4\u70b9\uff0c\u7edf\u8ba1\u8fd9\u4e9b+1\u548c-1\u7684\u603b\u548c\u3002\u6211\u4eec\u4f1a\u53d1\u73b0\u5f53sum\u4ece0\u53d8\u4e3a\u6b63\u6570\u65f6\uff0c\u610f\u5473\u7740\u4e00\u4e2amerged interval\u7684\u5f00\u59cb\uff1b\u5f53sum\u4ece\u6b63\u6570\u53d8\u62100\u65f6\uff0c\u610f\u5473\u7740\u4e00\u4e2amerged interval\u7684\u7ed3\u675f\u3002\u8fd9\u6837\u5c31\u5de7\u5999\u5730\u628a\u6240\u6709\u5b58\u5728overlap\u7684\u533a\u95f4\u90fd\u5408\u5e76\u5230\u4e86\u4e00\u8d77\u3002 \u9700\u8981\u6ce8\u610f\u7684\u662f\uff0c\u5bf9\u4e8e\u76f8\u540c\u7684\u65f6\u523b\uff0c\u5982\u679c\u540c\u65f6\u5b58\u5728\u591a\u4e2a+1\u6216\u8005-1\uff0c\u5e94\u8be5\u5148\u5904\u7406+1\u540e\u5904\u7406-\uff11\u3002\u6bd4\u5982[a,b]\u548c[b,c]\u4e24\u4e2a\u533a\u95f4\uff0c\u5728\u5904\u7406b\u65f6\u523b\u65f6\uff0c\u6309\u7167\u5148+1\u518d-1\u7684\u987a\u5e8f\uff0c\u5c31\u4e0d\u4f1a\u51fa\u73b0sum=0\u7684\u60c5\u51b5\u4e86\uff0c\u4e5f\u5c31\u907f\u514d\u4e86merged interval\u5728b\u5904\u65ad\u5f00\u3002 [Leetcode Link](https://leetcode.com/problems/merge-intervals)"
    }
  ]
}
