# @my-ds/components

Framework-agnostic custom elements for the educational design system.

```js
import "@my-ds/tokens/theme-default.css";
import "@my-ds/components/bundle.css";
import "@my-ds/components";
```

The package emits a custom elements manifest at `dist/custom-elements.json`; the docs use that manifest to render implementation documentation.
