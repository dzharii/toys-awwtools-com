# Radius

Radius tokens keep controls, surfaces, and badges visually consistent.

<TokenTable group="radius" />

## Usage

Use `--semantic-radius-control` for buttons and inputs, `--semantic-radius-surface` for cards and alerts, and `--semantic-radius-badge` for pill-shaped metadata.
