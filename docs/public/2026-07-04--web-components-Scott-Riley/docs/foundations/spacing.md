# Spacing

Spacing tokens provide a small scale plus semantic aliases for components.

<TokenTable group="space" />

## Usage

Use global spacing tokens for layout rhythm and semantic spacing tokens for repeated component decisions such as action padding and form field gaps.

```css
.example {
  gap: var(--global-space-md);
  padding: var(--semantic-space-card-padding);
}
```
