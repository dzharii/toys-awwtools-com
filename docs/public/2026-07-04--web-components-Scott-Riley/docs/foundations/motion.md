# Motion

Motion tokens keep interactive feedback predictable and restrained.

<TokenTable group="motion" />

## Reduced Motion

Component CSS respects `prefers-reduced-motion`. Loading indicators still communicate state, but transitions and rotations are softened so the interface remains usable for motion-sensitive users.
