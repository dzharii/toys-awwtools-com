# Article Source

The original article content for this project is preserved at `index.md` in the project root.

The implementation follows the article's practical arc:

1. Build a component library and documentation site together.
2. Make components primitive, semantic, and framework-agnostic.
3. Drive styling with code-defined tokens.
4. Generate implementation docs from component metadata.
5. Finish with a package that can be consumed outside the documentation site.

The inferred continuation plan is preserved at `plan-part2-part3.md`.
