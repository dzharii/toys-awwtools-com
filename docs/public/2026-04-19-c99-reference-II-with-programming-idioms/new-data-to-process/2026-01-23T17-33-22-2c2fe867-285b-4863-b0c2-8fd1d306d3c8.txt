Print Hello World
URL: [https://programming-idioms.org/idiom/1/print-hello-world](https://programming-idioms.org/idiom/1/print-hello-world)
Description: Print a literal string followed by a newline to the standard output stream using a generic formatted output function.

```c
#include <stdio.h>

int main(void) {
    printf("Hello World\n");
    return 0;
}
```

Print Hello 10 times
URL: [https://programming-idioms.org/idiom/2/print-hello-10-times](https://programming-idioms.org/idiom/2/print-hello-10-times)
Description: Execute the same output operation a fixed number of times using a deterministic loop.

```c
#include <stdio.h>

int main(void) {
    for (int i = 0; i < 10; ++i) {
        printf("Hello\n");
    }
    return 0;
}
```

Create a procedure
URL: [https://programming-idioms.org/idiom/3/create-a-procedure](https://programming-idioms.org/idiom/3/create-a-procedure)
Description: Define a function that returns no value and performs side effects only.

```c
#include <stdio.h>

void finish(void) {
    printf("My job here is done.\n");
}
```

Create a function
URL: [https://programming-idioms.org/idiom/4/create-a-function](https://programming-idioms.org/idiom/4/create-a-function)
Description: Define a function that computes and returns the square of an integer value.

```c
int square_int(int value) {
    return value * value;
}
```

Create a 2D Point data structure
URL: [https://programming-idioms.org/idiom/5/create-a-2d-point-data-structure](https://programming-idioms.org/idiom/5/create-a-2d-point-data-structure)
Description: Declare a lightweight structure representing a two-dimensional point using double-precision coordinates.

```c
typedef struct {
    double x;
    double y;
} Point2D;
```

Iterate over list values
URL: [https://programming-idioms.org/idiom/6/iterate-over-list-values](https://programming-idioms.org/idiom/6/iterate-over-list-values)
Description: Iterate over each element of an array and apply a processing function to each element, with all required context explicitly defined.

```c
#include <stddef.h>

typedef struct {
    int id;
} Item;

void process_item(Item* item) {
    item->id += 1;
}

void process_items(Item* items, size_t count) {
    for (size_t i = 0; i < count; ++i) {
        process_item(&items[i]);
    }
}
```

Iterate over list indexes and values
URL: [https://programming-idioms.org/idiom/7/iterate-over-list-indexes-and-values](https://programming-idioms.org/idiom/7/iterate-over-list-indexes-and-values)
Description: Iterate over an array while accessing both index and value in a type-safe and explicit manner.

```c
#include <stdio.h>
#include <stddef.h>

typedef struct {
    const char* name;
} Item;

const char* item_to_string(Item item) {
    return item.name;
}

void print_items(const Item* items, size_t count) {
    for (size_t i = 0; i < count; ++i) {
        printf("Item %zu = %s\n", i, item_to_string(items[i]));
    }
}
```

Create a map (associative array)
URL: [https://programming-idioms.org/idiom/8/create-a-map-associative-array](https://programming-idioms.org/idiom/8/create-a-map-associative-array)
Description: Represent an associative mapping using an array of key-value structures, which is portable and explicit in C.

```c
typedef struct {
    const char* key;
    const char* value;
} KeyValue;

KeyValue map[] = {
    { "foo", "twenty" },
    { "bar", "three" }
};
```

Create a Binary Tree data structure
URL: [https://programming-idioms.org/idiom/9/create-a-binary-tree-data-structure](https://programming-idioms.org/idiom/9/create-a-binary-tree-data-structure)
Description: Define a recursive structure for a binary tree node with left and right children.

```c
typedef struct TreeNode {
    int value;
    struct TreeNode* left;
    struct TreeNode* right;
} TreeNode;
```

Shuffle a list
URL: [https://programming-idioms.org/idiom/10/shuffle-a-list](https://programming-idioms.org/idiom/10/shuffle-a-list)
Description: Randomly permute the elements of an integer array using the Fisher-Yates algorithm.

```c
#include <stdlib.h>
#include <stddef.h>

void shuffle_ints(int* array, size_t count) {
    if (count < 2) {
        return;
    }

    for (size_t i = count - 1; i > 0; --i) {
        size_t j = (size_t)(rand() % (i + 1));
        int tmp = array[i];
        array[i] = array[j];
        array[j] = tmp;
    }
}
```

Pick a random element from a list
URL: [https://programming-idioms.org/idiom/11/pick-a-random-element-from-a-list](https://programming-idioms.org/idiom/11/pick-a-random-element-from-a-list)
Description: Select one element uniformly at random from a non-empty integer array.

```c
#include <stdlib.h>
#include <stddef.h>

int pick_random_int(const int* array, size_t count) {
    return array[rand() % count];
}
```

Check if list contains a value
URL: [https://programming-idioms.org/idiom/12/check-if-list-contains-a-value](https://programming-idioms.org/idiom/12/check-if-list-contains-a-value)
Description: Perform a linear search to determine whether an integer value exists in an array.

```c
#include <stdbool.h>
#include <stddef.h>

bool contains_int(const int* array, size_t count, int value) {
    for (size_t i = 0; i < count; ++i) {
        if (array[i] == value) {
            return true;
        }
    }
    return false;
}
```

Pick uniformly a random floating point number in [a..b)
URL: [https://programming-idioms.org/idiom/14/pick-uniformly-a-random-floating-point-number-in-a-b](https://programming-idioms.org/idiom/14/pick-uniformly-a-random-floating-point-number-in-a-b)
Description: Generate a floating point value greater than or equal to a and strictly less than b.

```c
#include <stdlib.h>

double random_double_range(double a, double b) {
    return a + (b - a) * ((double)rand() / (double)RAND_MAX);
}
```

Pick uniformly a random integer in [a..b]
URL: [https://programming-idioms.org/idiom/15/pick-uniformly-a-random-integer-in-a-b](https://programming-idioms.org/idiom/15/pick-uniformly-a-random-integer-in-a-b)
Description: Generate a uniformly distributed integer in a closed interval while avoiding modulo bias.

```c
#include <stdlib.h>

int random_int_range(int a, int b) {
    const int range = b - a + 1;
    const int limit = RAND_MAX - (RAND_MAX % range);
    int r;

    do {
        r = rand();
    } while (r >= limit);

    return a + (r % range);
}
```

