Declare an enumeration
URL: [https://programming-idioms.org/idiom/122/declare-an-enumeration](https://programming-idioms.org/idiom/122/declare-an-enumeration)
Description: Define an enumeration type with a fixed set of named integer constants.

```c
typedef enum {
    SPADES,
    HEARTS,
    DIAMONDS,
    CLUBS
} Suit;
```

Assert condition
URL: [https://programming-idioms.org/idiom/123/assert-condition](https://programming-idioms.org/idiom/123/assert-condition)
Description: Verify that a condition holds true during program execution in debug builds.

```c
#include <assert.h>

void check_consistency(int is_consistent) {
    assert(is_consistent);
}
```

Multiple return values
URL: [https://programming-idioms.org/idiom/126/multiple-return-values](https://programming-idioms.org/idiom/126/multiple-return-values)
Description: Return multiple values from a function by grouping them in a structure.

```c
#include <stdbool.h>

typedef struct {
    const char* text;
    bool ok;
} Result;

Result make_result(void) {
    Result r = { "Hello", true };
    return r;
}
```

Source code inclusion
URL: [https://programming-idioms.org/idiom/127/source-code-inclusion](https://programming-idioms.org/idiom/127/source-code-inclusion)
Description: Include source code from another file directly into a function body.

```c
void foo(void) {
#include "foobody.txt"
}
```

Depth-first traversal in a graph
URL: [https://programming-idioms.org/idiom/130/depth-first-traversal-in-a-graph](https://programming-idioms.org/idiom/130/depth-first-traversal-in-a-graph)
Description: Perform a depth-first traversal over a graph represented by adjacency lists.

```c
#include <stddef.h>

typedef struct Node {
    size_t index;
    struct Node** neighbors;
    size_t neighbor_count;
} Node;

void dfs(Node* node, int* visited) {
    if (visited[node->index]) {
        return;
    }

    visited[node->index] = 1;
    for (size_t i = 0; i < node->neighbor_count; ++i) {
        dfs(node->neighbors[i], visited);
    }
}
```

Successive conditions
URL: [https://programming-idioms.org/idiom/131/successive-conditions](https://programming-idioms.org/idiom/131/successive-conditions)
Description: Execute the first matching branch among several mutually exclusive conditions.

```c
void dispatch(int c1, int c2, int c3) {
    if (c1) {
        f1();
    } else if (c2) {
        f2();
    } else if (c3) {
        f3();
    }
}
```

Check if string contains only digits
URL: [https://programming-idioms.org/idiom/137/check-if-string-contains-only-digits](https://programming-idioms.org/idiom/137/check-if-string-contains-only-digits)
Description: Determine whether a string consists exclusively of decimal digit characters.

```c
#include <stdbool.h>
#include <ctype.h>

bool is_numeric(const char* s) {
    if (!*s) {
        return false;
    }

    while (*s) {
        if (!isdigit((unsigned char)*s)) {
            return false;
        }
        ++s;
    }
    return true;
}
```

Create temp file
URL: [https://programming-idioms.org/idiom/138/create-temp-file](https://programming-idioms.org/idiom/138/create-temp-file)
Description: Create a temporary file using a secure, system-provided mechanism.

```c
#include <stdlib.h>

FILE* create_temp_file(void) {
    return tmpfile();
}
```

Hexadecimal digits of an integer
URL: [https://programming-idioms.org/idiom/142/hexadecimal-digits-of-an-integer](https://programming-idioms.org/idiom/142/hexadecimal-digits-of-an-integer)
Description: Convert an integer value to its hexadecimal string representation.

```c
#include <stdio.h>
#include <stddef.h>

void int_to_hex(unsigned int value, char* buffer, size_t buffer_size) {
    snprintf(buffer, buffer_size, "%x", value);
}
```

Check if file exists
URL: [https://programming-idioms.org/idiom/144/check-if-file-exists](https://programming-idioms.org/idiom/144/check-if-file-exists)
Description: Check whether a file exists by attempting to open it.

```c
#include <stdio.h>
#include <stdbool.h>

bool file_exists(const char* path) {
    FILE* file = fopen(path, "r");
    if (!file) {
        return false;
    }
    fclose(file);
    return true;
}
```

Turn a character into a string
URL: [https://programming-idioms.org/idiom/152/turn-a-character-into-a-string](https://programming-idioms.org/idiom/152/turn-a-character-into-a-string)
Description: Create a null-terminated string containing a single character.

```c
void char_to_string(char c, char* buffer) {
    buffer[0] = c;
    buffer[1] = '\0';
}
```

Delete file
URL: [https://programming-idioms.org/idiom/155/delete-file](https://programming-idioms.org/idiom/155/delete-file)
Description: Remove a file from the filesystem.

```c
#include <stdio.h>

int delete_file(const char* path) {
    return remove(path);
}
```

Declare constant string
URL: [https://programming-idioms.org/idiom/157/declare-constant-string](https://programming-idioms.org/idiom/157/declare-constant-string)
Description: Declare a constant string literal.

```c
const char* planet = "Earth";
```

Multiply all the elements of a list
URL: [https://programming-idioms.org/idiom/161/multiply-all-the-elements-of-a-list](https://programming-idioms.org/idiom/161/multiply-all-the-elements-of-a-list)
Description: Multiply each element of an integer array by a constant factor.

```c
#include <stddef.h>

void scale_ints(int* array, size_t count, int factor) {
    for (size_t i = 0; i < count; ++i) {
        array[i] *= factor;
    }
}
```

Execute procedures depending on options
URL: [https://programming-idioms.org/idiom/162/execute-procedures-depending-on-options](https://programming-idioms.org/idiom/162/execute-procedures-depending-on-options)
Description: Execute different procedures based on parsed command-line options.

```c
#include <unistd.h>

int main(int argc, char* argv[]) {
    int opt;
    while ((opt = getopt(argc, argv, "bf")) != -1) {
        if (opt == 'b') {
            bat();
        } else if (opt == 'f') {
            fox();
        }
    }
    return 0;
}
```

Print list elements by group of 2
URL: [https://programming-idioms.org/idiom/163/print-list-elements-by-group-of-2](https://programming-idioms.org/idiom/163/print-list-elements-by-group-of-2)
Description: Print elements of an integer array two at a time.

```c
#include <stdio.h>
#include <stddef.h>

void print_pairs(const int* array, size_t count) {
    for (size_t i = 0; i + 1 < count; i += 2) {
        printf("%d, %d\n", array[i], array[i + 1]);
    }
}
```

Last element of list
URL: [https://programming-idioms.org/idiom/165/last-element-of-list](https://programming-idioms.org/idiom/165/last-element-of-list)
Description: Retrieve the last element of a non-empty array.

```c
int last_element(const int* array, size_t count) {
    return array[count - 1];
}
```

Trim prefix
URL: [https://programming-idioms.org/idiom/167/trim-prefix](https://programming-idioms.org/idiom/167/trim-prefix)
Description: Remove a prefix from a string if it is present.

```c
#include <string.h>

const char* trim_prefix(const char* s, const char* prefix) {
    size_t len = strlen(prefix);
    return strncmp(s, prefix, len) == 0 ? s + len : s;
}
```

Format a number with grouped thousands
URL: [https://programming-idioms.org/idiom/173/format-a-number-with-grouped-thousands](https://programming-idioms.org/idiom/173/format-a-number-with-grouped-thousands)
Description: Format an integer with thousands separators using the current locale.

```c
#include <stdio.h>
#include <locale.h>

void print_grouped(int value) {
    setlocale(LC_ALL, "");
    printf("%'d\n", value);
}
```

Bytes to hex string
URL: [https://programming-idioms.org/idiom/175/bytes-to-hex-string](https://programming-idioms.org/idiom/175/bytes-to-hex-string)
Description: Convert a byte array into a hexadecimal string representation.

```c
#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>

char* bytes_to_hex(const unsigned char* bytes, size_t count) {
    char* hex = (char*)malloc(count * 2 + 1);
    if (!hex) {
        return NULL;
    }

    for (size_t i = 0; i < count; ++i) {
        sprintf(hex + i * 2, "%02x", bytes[i]);
    }
    return hex;
}
```

Hex string to byte array
URL: [https://programming-idioms.org/idiom/176/hex-string-to-byte-array](https://programming-idioms.org/idiom/176/hex-string-to-byte-array)
Description: Convert a hexadecimal string into a byte array.

```c
#include <stdlib.h>
#include <ctype.h>

unsigned char* hex_to_bytes(const char* hex, size_t* out_count) {
    size_t len = strlen(hex) / 2;
    unsigned char* bytes = (unsigned char*)malloc(len);
    if (!bytes) {
        return NULL;
    }

    for (size_t i = 0; i < len; ++i) {
        unsigned int value;
        sscanf(hex + i * 2, "%2x", &value);
        bytes[i] = (unsigned char)value;
    }

    *out_count = len;
    return bytes;
}
```

Check if point is inside rectangle
URL: [https://programming-idioms.org/idiom/178/check-if-point-is-inside-rectangle](https://programming-idioms.org/idiom/178/check-if-point-is-inside-rectangle)
Description: Determine whether a point lies inside or on the boundary of an axis-aligned rectangle.

```c
#include <stdbool.h>

bool point_in_rect(
    double x1,
    double y1,
    double x2,
    double y2,
    double px,
    double py
) {
    return px >= x1 && px <= x2 && py >= y1 && py <= y2;
}
```

List files in directory
URL: [https://programming-idioms.org/idiom/180/list-files-in-directory](https://programming-idioms.org/idiom/180/list-files-in-directory)
Description: List entries in a directory without recursion.

```c
#include <dirent.h>

int list_directory(const char* path) {
    DIR* dir = opendir(path);
    if (!dir) {
        return -1;
    }

    struct dirent* entry;
    while ((entry = readdir(dir)) != NULL) {
        printf("%s\n", entry->d_name);
    }

    closedir(dir);
    return 0;
}
```

