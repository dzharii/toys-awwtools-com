Conditional assignment
URL: [https://programming-idioms.org/idiom/252/conditional-assignment](https://programming-idioms.org/idiom/252/conditional-assignment)
Description: Assign one of two string values depending on a boolean condition.

```c
const char* choose_value(int condition) {
    return condition ? "a" : "b";
}
```

Count backwards
URL: [https://programming-idioms.org/idiom/256/count-backwards](https://programming-idioms.org/idiom/256/count-backwards)
Description: Print integers in descending order from a fixed starting value to zero.

```c
#include <stdio.h>

void count_down(void) {
    for (int i = 5; i >= 0; --i) {
        printf("%d\n", i);
    }
}
```

Count trailing zero bits
URL: [https://programming-idioms.org/idiom/262/count-trailing-zero-bits](https://programming-idioms.org/idiom/262/count-trailing-zero-bits)
Description: Count the number of consecutive zero bits starting from the least significant bit.

```c
#include <stddef.h>

size_t count_trailing_zeros(unsigned int value) {
    if (value == 0) {
        return sizeof(value) * 8;
    }

    size_t count = 0;
    while ((value & 1U) == 0) {
        value >>= 1;
        ++count;
    }
    return count;
}
```

Repeated string
URL: [https://programming-idioms.org/idiom/266/repeated-string](https://programming-idioms.org/idiom/266/repeated-string)
Description: Create a new string consisting of a base string repeated a fixed number of times.

```c
#include <stdlib.h>
#include <string.h>

char* repeat_string(const char* value, size_t times) {
    size_t len = strlen(value);
    char* result = (char*)malloc(len * times + 1);
    if (!result) {
        return NULL;
    }

    char* p = result;
    for (size_t i = 0; i < times; ++i) {
        memcpy(p, value, len);
        p += len;
    }
    result[len * times] = '\0';
    return result;
}
```

Binary digits to byte array
URL: [https://programming-idioms.org/idiom/275/binary-digits-to-byte-array](https://programming-idioms.org/idiom/275/binary-digits-to-byte-array)
Description: Convert a string of binary digits into a byte array.

```c
#include <stdlib.h>
#include <string.h>

unsigned char* binary_to_bytes(const char* bits, size_t* out_count) {
    size_t len = strlen(bits);
    size_t count = len / 8;
    unsigned char* bytes = (unsigned char*)calloc(count, 1);
    if (!bytes) {
        return NULL;
    }

    for (size_t i = 0; i < len; ++i) {
        if (bits[i] == '1') {
            bytes[i / 8] |= 1U << (7 - (i % 8));
        }
    }

    *out_count = count;
    return bytes;
}
```

Create a zeroed list of integers
URL: [https://programming-idioms.org/idiom/284/create-a-zeroed-list-of-integers](https://programming-idioms.org/idiom/284/create-a-zeroed-list-of-integers)
Description: Allocate an integer array initialized with zeros.

```c
#include <stdlib.h>

int* create_zeroed_ints(size_t count) {
    return (int*)calloc(count, sizeof(int));
}
```

Comment out a single line
URL: [https://programming-idioms.org/idiom/299/comment-out-a-single-line](https://programming-idioms.org/idiom/299/comment-out-a-single-line)
Description: Add a comment line that is ignored by the compiler.

```c
// This is a comment.
```

String interpolation
URL: [https://programming-idioms.org/idiom/302/string-interpolation](https://programming-idioms.org/idiom/302/string-interpolation)
Description: Format a string by inserting an integer value into a text template.

```c
#include <stdio.h>
#include <stddef.h>

void format_planets(int count, char* buffer, size_t buffer_size) {
    snprintf(buffer, buffer_size, "Our sun has %d planets", count);
}
```

Calculate exponentiation of real numbers
URL: [https://programming-idioms.org/idiom/305/calculate-exponentiation-of-real-numbers](https://programming-idioms.org/idiom/305/calculate-exponentiation-of-real-numbers)
Description: Compute real-number exponentiation using the math library.

```c
#include <math.h>
#include <stdio.h>

void print_powers(double a, double b, int n) {
    printf("%f %f\n", pow(a, b), pow(a, n));
}
```

Test if string is empty
URL: [https://programming-idioms.org/idiom/320/test-if-string-is-empty](https://programming-idioms.org/idiom/320/test-if-string-is-empty)
Description: Check whether a string has zero length.

```c
#include <stdbool.h>
#include <string.h>

bool is_empty(const char* s) {
    return s[0] == '\0';
}
```

Convert string to integer, explicit base
URL: [https://programming-idioms.org/idiom/337/convert-string-to-integer-explicit-base](https://programming-idioms.org/idiom/337/convert-string-to-integer-explicit-base)
Description: Convert a string to an integer using an explicit numeric base.

```c
#include <stdlib.h>

int string_to_int_base(const char* s, int base) {
    return (int)strtol(s, NULL, base);
}
```

Rename file
URL: [https://programming-idioms.org/idiom/343/rename-file](https://programming-idioms.org/idiom/343/rename-file)
Description: Rename a file on the filesystem.

```c
#include <stdio.h>

int rename_file(const char* from, const char* to) {
    return rename(from, to);
}
```

