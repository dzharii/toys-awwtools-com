# Source User Request

Captured from the user request that initiated this project.

```text
Switch to the `nob-boilerplate-generator` folder and follow `WORK_PROTOCOL_V001.md` and `WORK_PROTOCOL_CHECKLIST_V001.md` strictly from start to finish: research with ChatGPT through Chrome, brainstorm, create micromanagement style specifications, save all artifacts locally, obtain final milestone advice, and only then implement autonomously. The product idea is a static website built with plain HTML, CSS, and JavaScript that acts as a power-user interactive code generator for `nob.h`, which is a single-header C build-system helper used to write build scripts in C instead of Make/CMake/shell scripts. The website should help users configure a `nob.c` bootstrap file for their specific project by changing options in the UI and immediately regenerating the corresponding boilerplate code in JavaScript, so the user can explore build-script options, customize project settings, and copy a ready starting point for their own `nob.h`-based build workflow.
```

