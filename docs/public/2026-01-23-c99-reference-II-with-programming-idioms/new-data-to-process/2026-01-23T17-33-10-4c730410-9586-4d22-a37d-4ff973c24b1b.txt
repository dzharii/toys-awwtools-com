Create a Tree data structure
URL: [https://programming-idioms.org/idiom/17/create-a-tree-data-structure](https://programming-idioms.org/idiom/17/create-a-tree-data-structure)
Description: Define a recursive tree node structure where each node may have zero or more children and access to its siblings.

```c
typedef struct TreeNode {
    int value;
    struct TreeNode* first_child;
    struct TreeNode* next_sibling;
} TreeNode;
```

Reverse a list
URL: [https://programming-idioms.org/idiom/19/reverse-a-list](https://programming-idioms.org/idiom/19/reverse-a-list)
Description: Reverse the elements of an integer array in place.

```c
#include <stddef.h>

void reverse_ints(int* array, size_t count) {
    size_t left = 0;
    size_t right = count ? count - 1 : 0;

    while (left < right) {
        int tmp = array[left];
        array[left] = array[right];
        array[right] = tmp;
        ++left;
        --right;
    }
}
```

Return two values
URL: [https://programming-idioms.org/idiom/20/return-two-values](https://programming-idioms.org/idiom/20/return-two-values)
Description: Search a 2D integer matrix for a value and return its coordinates using output parameters.

```c
#include <stddef.h>
#include <stdbool.h>

bool find_in_matrix(
    const int* matrix,
    size_t rows,
    size_t cols,
    int target,
    size_t* out_row,
    size_t* out_col
) {
    for (size_t r = 0; r < rows; ++r) {
        for (size_t c = 0; c < cols; ++c) {
            if (matrix[r * cols + c] == target) {
                *out_row = r;
                *out_col = c;
                return true;
            }
        }
    }
    return false;
}
```

Swap values
URL: [https://programming-idioms.org/idiom/21/swap-values](https://programming-idioms.org/idiom/21/swap-values)
Description: Swap the values of two integers using a temporary variable.

```c
void swap_ints(int* a, int* b) {
    int tmp = *a;
    *a = *b;
    *b = tmp;
}
```

Convert string to integer
URL: [https://programming-idioms.org/idiom/22/convert-string-to-integer](https://programming-idioms.org/idiom/22/convert-string-to-integer)
Description: Convert a decimal string to an integer with basic error handling.

```c
#include <stdlib.h>

int string_to_int(const char* s) {
    return (int)strtol(s, NULL, 10);
}
```

Convert real number to string with 2 decimal places
URL: [https://programming-idioms.org/idiom/23/convert-real-number-to-string-with-2-decimal-places](https://programming-idioms.org/idiom/23/convert-real-number-to-string-with-2-decimal-places)
Description: Format a floating point value into a string with exactly two digits after the decimal point.

```c
#include <stdio.h>

void format_two_decimals(double value, char* buffer, size_t buffer_size) {
    snprintf(buffer, buffer_size, "%.2f", value);
}
```

Assign to string the japanese word ネコ
URL: [https://programming-idioms.org/idiom/24/assign-to-string-the-japanese-word](https://programming-idioms.org/idiom/24/assign-to-string-the-japanese-word)
Description: Initialize a UTF-8 string literal containing non-ASCII characters.

```c
const char* s = "ネコ";
```

Create a 2-dimensional array
URL: [https://programming-idioms.org/idiom/26/create-a-2-dimensional-array](https://programming-idioms.org/idiom/26/create-a-2-dimensional-array)
Description: Declare a two-dimensional array of double values using C99 variable length arrays.

```c
void example_matrix(size_t rows, size_t cols) {
    double matrix[rows][cols];
}
```

Create a 3-dimensional array
URL: [https://programming-idioms.org/idiom/27/create-a-3-dimensional-array](https://programming-idioms.org/idiom/27/create-a-3-dimensional-array)
Description: Declare a three-dimensional array of double values using C99 variable length arrays.

```c
void example_tensor(size_t x, size_t y, size_t z) {
    double tensor[x][y][z];
}
```

Sort by a property
URL: [https://programming-idioms.org/idiom/28/sort-by-a-property](https://programming-idioms.org/idiom/28/sort-by-a-property)
Description: Sort an array of structures by an integer field using qsort and a comparator.

```c
#include <stdlib.h>

typedef struct {
    int priority;
} Item;

int compare_item_priority(const void* a, const void* b) {
    const Item* ia = (const Item*)a;
    const Item* ib = (const Item*)b;

    if (ia->priority < ib->priority) {
        return -1;
    }
    if (ia->priority > ib->priority) {
        return 1;
    }
    return 0;
}

void sort_items(Item* items, size_t count) {
    qsort(items, count, sizeof(Item), compare_item_priority);
}
```

