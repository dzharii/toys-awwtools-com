{
  "name": "huge-page-object-recorder-bookmarklet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bookmarklet-style page object recorder for browser automation discovery.",
  "scripts": {
    "build": "bun run scripts/build.mjs",
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=text --coverage-reporter=lcov",
    "check": "bun run build && bun run test:coverage"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.8.9"
  }
}
